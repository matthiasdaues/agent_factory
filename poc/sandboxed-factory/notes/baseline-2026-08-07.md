# Comparison note — Candidate 1: Baseline (host-native subprocess)

Date: 2026-08-07
Runtime: host uid 1000 (matthiasdaues), Ubuntu 22.04, kernel 6.8.

## Definition-of-done results

- [x] Host secret paths are **readable** from the agent process.
  - `/etc/shadow` → READABLE
  - `~/.aws` → READABLE
  - `~/.ssh` → READABLE
  - `~/.config` → READABLE
  - (uid 1000, same owner as these paths — trivially reachable)
- [x] Arbitrary outbound egress **succeeds** from the agent process.
  - `curl https://example.com` → HTTP 200
- [x] One-line summary of effort/risk this baseline commits to.

## Effort / risk committed

- **Effort:** zero — this is the status quo; nothing to operate.
- **Risk:** the orchestrator's subprocess (and any agent it launches) runs as the
  calling user with full reach over user secrets and the network. Any compromise,
  prompt injection, or botched gate result carries host-user privileges and
  full egress. This is the precise gap the containerized candidate must close.

## Verdict (for Step 4)

Baseline confirms the premise: no host-fs or egress boundary today. Serves as
the control for the candidate.
