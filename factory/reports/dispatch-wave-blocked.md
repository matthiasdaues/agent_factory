# Blocked dispatch wave

This canonical tracked report is replaced by `dispatch_wave` when one or more
wave items are blocked. The generated report contains bounded per-item
transport diagnostics so the BR-040 aggregate envelope always points to a
durable artifact.
