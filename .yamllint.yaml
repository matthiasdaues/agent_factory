---
extends: relaxed

rules:
  line-length:
    max: 500
    allow-non-breakable-inline-mappings: true
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  document-start: enable
