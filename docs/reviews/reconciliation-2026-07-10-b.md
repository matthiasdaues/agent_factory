# Reconciliation Report — 2026-07-10-b

## Scope

- Branch: `feature/integrate-orchestrator`, range `a6006f9..1c90209` (7 commits, base inclusive) — two purely additive orchestration playbooks (`factory/playbooks/poc-spike.md`, `factory/playbooks/technical-poc.md`, no code yet exists to drift against), followed by the fix pass for all 5 findings filed in the prior `reconciliation-2026-07-10.md` pass, and a newly-filed finding documenting a stale-path test-collection failure.
- Code compared: `factory/scripts/merge-precommit-config` (+ new `orchestrator/tests/test_merge_precommit_config.py`), `orchestrator/src/orchestrator/cli.py`, `orchestrator/.pre-commit-config.yaml`, `factory/agents/*.md` (8 files), `factory/rulebooks/conventions/branching-policy.md`.
- Docs compared: the two new playbooks' cross-references against their targets; all 5 finding files' `status`/fix claims against their fix commits; `docs/findings/RECON-0001-0004`, `orchestrator/docs/findings/RECON-0003-0004`.
- Confirmed present and intentionally out of scope, not re-flagged: the new `docs/findings/RECON-0004.md` (root) — deferred by explicit user decision to a future YAGNI-inspired codebase review, left `status: open`; the pre-existing 19 `arch-lint` ADR-frontmatter errors and 2 `backlog-lint` warnings on `orchestrator/`; two additional pre-existing (not introduced by this range, confirmed by testing at `a6006f9~1`) test failures in `test_classification_resolution.py` and `test_model_matrix_views.py` that share `RECON-0004`'s stale-`scripts/`-path root cause but are outside the two files that finding names — consistent with that finding's own note that other silent occurrences may exist, and out of scope per the same deferral.

## Discrepancy table

| #   | Classification | Artifact                                                       | Finding                                                                                                                                                                                                                                                                                                                                                                                                                            | Action taken                                                                             |
| --- | -------------- | -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 1   | Code defect    | `factory/playbooks/technical-poc.md#L60,L63` (new, this range) | Two links anchor into `## Step 1 — Title` headings (`pugh-matrix/SKILL.md`, `write-adr/SKILL.md`) using a single hyphen where GitHub's actual slugger (verified against the `github-slugger` package) produces a double hyphen for the em dash. Both land on the right file, wrong section. Not caught by the pre-commit manual check because `cross-reference-format.md` enforcement is "none — human/agent-authored convention." | Filed `docs/findings/RECON-0005.md` (minor). Not fixed — handed to implementation agent. |

Also checked and clean, no findings:

- **Two new playbooks overall** — `poc-spike.md`'s one link (`feature-addition.md`, no anchor) resolves; `technical-poc.md`'s other 6 links (`poc-spike.md`, `feature-addition.md` ×2 each, `implementation-agent.md#branching-model`, `grill-with-docs/SKILL.md`, `domain-modeling/SKILL.md#offer-adrs-sparingly`) all resolve to existing files and, where anchored, the correct heading slug — confirmed by direct heading grep and, for the two ambiguous em-dash cases above, the actual GitHub slugger. Frontmatter (`title`, `category`, `type`, `scenario`, `version`) matches the pattern of existing playbooks.
- **`RECON-0001` (root, `merge-precommit-config`)** — Fix commit `6378652` matches the finding exactly: `extract_marker_id()` derives the no-op marker from `--template`'s own first `- id:` line instead of the hardcoded `"id: index-lint"`. New `orchestrator/tests/test_merge_precommit_config.py` (5 tests, all passing) covers both splice directions including the exact regression scenario the finding described (target already has the factory marker, template is a different, orchestrator-shaped block) — matches the finding's requested test coverage precisely.
- **`RECON-0002` (root, broken `../factory/rulebooks/` links)** — Fix commit `01d579f` mechanically replaces `../factory/rulebooks/conventions/` with `../rulebooks/conventions/` across all 8 named files. Re-ran `grep -rln '\.\./factory/rulebooks' factory/agents/`: zero remaining hits. No new broken links introduced by the fix (spot-checked resulting paths resolve from each file's real location).
- **`RECON-0003` (root, bare code-span in `branching-policy.md`)** — Fix commit `87a996d` replaces the bare code span with `[implementation-agent.md § Workflow, Step 3 (...)](../../agents/implementation-agent.md#workflow)`. Deliberately anchors at the plain `## Workflow` heading rather than the numbered `Step 3` list item nested under it (Step 3 is a list item, not its own heading) — a safer choice than the finding's own suggested anchor, which explicitly left room to "adjust the anchor slug to match the actual rendered heading id." Verified: `../../agents/implementation-agent.md` resolves correctly from `factory/rulebooks/conventions/`, and `#workflow` matches the real heading with no em-dash ambiguity.
- **`RECON-0003` (orchestrator, `.pre-commit-config.yaml` sync)** — Fix commit `12da219` updates all four gate hooks (`arch-lint`, `backlog-lint`, `matrix-lint`, `statemachine-lint`) to `python3 factory/scripts/<name>` with matching flags, and adds a header note explaining why the dev-only config still exists alongside root's `-orchestrator`-suffixed hooks — both parts of the finding's two-part fix request. Diffed all four entries byte-for-byte against both the already-fixed template (`orchestrator/pre-commit-config.yaml`) and root `.pre-commit-config.yaml`'s `-orchestrator` hooks: exact match.
- **`RECON-0004` (orchestrator, "Agent HQ" strings in `cli.py`)** — Fix commit `cf9fd70` replaces both named occurrences (line 2304 instruction-file template, line 2653 error message) exactly as the finding specified. Re-grepped `orchestrator/src/` and the rest of the repo's `.py` files for any remaining literal "Agent HQ": zero hits — the finding's own closing instruction to check for stragglers was honoured. (Pre-existing "Agent HQ" references remain throughout `orchestrator/docs/` prose — PRD, glossary, several ADRs — but these predate this range, are out of this fix's stated scope, and are the same class of terminology drift the prior report's row 8 already deferred to `ST-0064`'s broader cleanup; not re-flagged here.)
- **New `docs/findings/RECON-0004.md` (root, stale `scripts/` path)** — Confirmed correctly filed: frontmatter (`id`, `source`, `severity: major`, `category: defect`, `artifact: orchestrator/tests/test_backlog_lint.py:20`, `status: open`) is well-formed, and its factual claim checks out — `uv run pytest --collect-only` inside `orchestrator/` reproduces exactly "2 errors during collection" against `test_backlog_lint.py` and `test_matrix_lint.py`, both `FileNotFoundError` on a bare `scripts/<name>` path. Left untouched and unfixed per explicit user instruction (deferred to a future codebase review); not re-flagged as a gap.
- **No new drift from the fix commits themselves** — Full orchestrator suite run (`pytest`, excluding the two files `RECON-0004` already names) : 757 passed, 3 pre-existing failures, 1 skipped. The 3 failures (`test_classification_resolution.py::test_developer_agent_definition_declares_no_tier`, `test_model_matrix_views.py::TestValidate::test_default_lint_fn_runs_the_real_matrix_lint_script`, `...test_default_lint_fn_surfaces_real_errors`) were reproduced identically at `a6006f9~1` (before any commit in this range), confirming they predate the range and are not a side effect of any of the 5 fixes.

## Spec files updated

None — no existing spec/architecture file was found stale against code in this range; the two new playbooks describe a workflow with no code counterpart to drift against (by design, per the task's own framing).

## Code defects filed

- `docs/findings/RECON-0005.md` (minor) — two broken em-dash anchors in the new `factory/playbooks/technical-poc.md`, introduced by this range's own additive commit and not caught by the pre-commit manual cross-reference check.

## Prior findings verified

- `docs/findings/RECON-0001.md`, `RECON-0002.md`, `RECON-0003.md` (root) — all `status: resolved`; fixes verified against each finding's "Fix:" text and confirmed no regressions. See discrepancy table above.
- `orchestrator/docs/findings/RECON-0003.md`, `RECON-0004.md` — both `status: resolved`; fixes verified. See discrepancy table above.
- `docs/findings/RECON-0004.md` (root) — `status: open` by design, confirmed correctly filed and factually accurate; not re-flagged, not fixed, per explicit user instruction.

## Linter results

- Root `spec-lint`: exit non-zero, "spec directory not found at docs/spec" — expected/inert, unchanged from prior pass (root `docs/spec/` still doesn't exist per `docs/CONTEXT-MAP.md`).
- Root `arch-lint --docs-dir docs --no-validate`: 0 errors, 0 warnings, 0 info (checked both before and after filing `RECON-0005.md`).
- Root `backlog-lint --backlog-dir backlog`: 0 errors, 0 warnings, 0 info across 8 story files.
- Orchestrator `spec-lint --spec-dir orchestrator/docs/spec`: 0 errors, 13 warnings, 18 info — same pre-existing EARS-heuristic and unreferenced-BR notices as the prior pass, unrelated to this range.
- Orchestrator `arch-lint --docs-dir orchestrator/docs --no-validate`: 19 errors (exactly the known pre-existing ADR-frontmatter gap on `0001`-`0019`, unchanged) — 0 warnings, 0 info.
- Orchestrator `backlog-lint --backlog-dir orchestrator/backlog`: 0 errors, 2 warnings across 66 story files (both on out-of-scope stories `ST-0059`/`ST-0061`, pre-existing, unchanged from prior pass).
- Orchestrator `pytest --collect-only`: "2 errors during collection" (`test_backlog_lint.py`, `test_matrix_lint.py`) — matches `RECON-0004`'s (root) claim exactly, unchanged, left open by design.
- Orchestrator `pytest` (excluding the two collection-error files): 757 passed, 3 failed (pre-existing, confirmed unrelated to this range — see discrepancy table), 1 skipped.
