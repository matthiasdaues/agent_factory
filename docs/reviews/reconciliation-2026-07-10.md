# Reconciliation Report — 2026-07-10

## Scope

- Branch: `feature/integrate-orchestrator`, range `e8a8565..40ba2db` (16 commits) — the full orchestrator-reintegration effort: bringing `orchestrator/` back as a permanent nested sub-project with `docs/` moved back to `orchestrator/docs/`; establishing root `backlog/` and root `docs/` as homes for whole-repo, cross-cutting concerns; de-symlinking and merging root `.pre-commit-config.yaml`; a `cli.py` path-resolution fix; folding `orchestrator/USAGE.md` into root `README.md`; and four process-improvement edits to `factory/` skill and agent definitions.
- Code compared: `orchestrator/src/`, `factory/`, the merged `.pre-commit-config.yaml`.
- Docs compared: `orchestrator/docs/` (arc42 chapters, ADRs, `CONTEXT.md`), root `docs/` (`CONTEXT-MAP.md`, `docs/adr/0001-precommit-monorepo-scoping.md`, `README.md`), root `backlog/ST-0001`–`ST-0008`, `orchestrator/backlog/ST-0062,64,65,66,67,68`.
- Confirmed present and intentionally out of scope, not re-flagged: 19 pre-existing `arch-lint` errors on `orchestrator/docs/adr/0001`–`0019` (missing YAML frontmatter); the `ADR-0003` traces-field collision in `backlog/ST-0003.md` and `orchestrator/backlog/ST-0067.md`; `orchestrator/backlog/ST-0064`'s own stale-layout todo flag (T-41); `ST-0004` left `status: pending` by design.

## Discrepancy table

| #   | Classification    | Artifact                                               | Finding                                                                                                                                                                                                                                                                                                                                                                                 | Action taken                                                                                                                                                       |
| --- | ----------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Spec stale        | `README.md` § Project Directory Tree                   | Tree still described root `.pre-commit-config.yaml` as a symlink (ST-0067 de-symlinked it), listed `docs/spec/`/`docs/findings/` (don't exist at root) while omitting `CONTEXT-MAP.md`/`reviews/` (which do), and omitted root `backlog/` entirely.                                                                                                                                     | Fixed — tree and pre-commit line updated to match current disk state.                                                                                              |
| 2   | Spec stale        | `.pre-commit-config.yaml` header comment               | "See ADR-0003." — bare, unlinked, numerically collides with orchestrator's own `docs/adr/0003-pre-commit-as-gate-bus.md` (a different decision).                                                                                                                                                                                                                                        | Fixed — now points at `docs/adr/0001-precommit-monorepo-scoping.md` with an explicit note on the numbering collision.                                              |
| 3   | Code defect       | `orchestrator/.pre-commit-config.yaml` (4 hooks)       | Dev config's own header claims it's kept in sync with its template, but ST-0066 fixed the template's `arch-lint`/`backlog-lint`/`matrix-lint`/`statemachine-lint` entries to `factory/scripts/<name>` while explicitly leaving this file's bare `scripts/<name>` entries untouched — all four are broken (no bare `scripts/` exists anywhere in the repo).                              | Filed `orchestrator/docs/findings/RECON-0003.md` (major). Not fixed — handed to implementation agent.                                                              |
| 4   | Code defect       | `orchestrator/src/orchestrator/cli.py:2304,2653`       | ST-0065's "Agent HQ" → "agent_factory" rename missed two strings: one shipped verbatim into every target project's instruction file, one in an error message.                                                                                                                                                                                                                           | Filed `orchestrator/docs/findings/RECON-0004.md` (major). Not fixed — handed to implementation agent.                                                              |
| 5   | Code defect       | `factory/scripts/merge-precommit-config:45`            | `docs/adr/0001-precommit-monorepo-scoping.md` documents this script as usable in both splice directions (factory-into-project and the new subproject-into-root), but its no-op marker is hardcoded to `"id: index-lint"` — invoking it in the new direction against root's file (which already contains that marker from the original splice) would silently no-op instead of splicing. | Filed `docs/findings/RECON-0001.md` (major, first entry in a new root `docs/findings/`). Not fixed — handed to implementation agent.                               |
| 6   | Code defect       | `factory/agents/*.md` (8 files)                        | Pre-existing, repo-wide broken relative link pattern (`../factory/rulebooks/conventions/...` resolves to a nonexistent `factory/factory/...` path from `factory/agents/`). Not introduced by this range, but ST-0005 and ST-0008 each added one more instance of the same broken pattern to `implementation-agent.md` (lines 50, 55) instead of catching it.                            | Filed `docs/findings/RECON-0002.md` (minor, scoped to the full 8-file pattern with the in-range additions called out). Not fixed — handed to implementation agent. |
| 7   | Code defect       | `factory/rulebooks/conventions/branching-policy.md:26` | New "Worktree Isolation" section (ST-0005) cites `implementation-agent.md` via a bare code span, violating this repo's own `cross-reference-format.md` MUST rule; the path is also wrong for the file's location.                                                                                                                                                                       | Filed `docs/findings/RECON-0003.md` (minor). Not fixed — handed to implementation agent.                                                                           |
| 8   | Terminology drift | `orchestrator/CONTEXT.md:7`                            | Glossary still defines Orchestrator as driving "the `ai_tooling` agent chain" — a pre-ADR-0010 term, two generations stale (confirmed dead by `orchestrator/tests/test_init.py::test_no_ai_tooling_entry`). Content predates this branch (restored wholesale, not authored fresh).                                                                                                      | Left as-is by user decision — folds into `ST-0064`'s existing broader stale-terminology todo rather than a one-off fix.                                            |

Also checked and clean, no findings: orchestrator arc42 component names against `orchestrator/src/` (exact match, no renamed/moved modules); all six orchestrator stories' (`ST-0062/64/65/66/67/68`) declared `outputs:` exist on disk and match their `status: done` claims; `orchestrator/USAGE.md` deletion (no dangling links repo-wide, `README.md`'s "Next steps" section covers what `ST-0068` required); `ST-0006`/`ST-0007` edits (retrospective/coaching-agent backlog-story tracking, domain-modeling multi-context checklist) against their acceptance criteria and cross-reference links; prior `RECON-0001`/`RECON-0002` (`orchestrator/docs/findings/`) — both already `resolved` from a pass before this range, spot-checked against `approval_service.py` (re-added wholesale, unchanged in substance) and still hold.

## Spec files updated

- `README.md` — Project Directory Tree: pre-commit line, `docs/` subtree, added root `backlog/` entry.
- `.pre-commit-config.yaml` — header comment now unambiguously references `docs/adr/0001-precommit-monorepo-scoping.md` instead of the colliding bare "ADR-0003".

## Code defects filed

- `orchestrator/docs/findings/RECON-0003.md` (major) — `orchestrator/.pre-commit-config.yaml` broken hook paths, out of sync with its own template.
- `orchestrator/docs/findings/RECON-0004.md` (major) — two leftover "Agent HQ" strings in `cli.py` missed by ST-0065's rename.
- `docs/findings/RECON-0001.md` (major) — `merge-precommit-config`'s hardcoded no-op marker breaks the bidirectional splice `docs/adr/0001-precommit-monorepo-scoping.md` documents.
- `docs/findings/RECON-0002.md` (minor) — repo-wide broken `../factory/rulebooks/...` link pattern across 8 `factory/agents/*.md` files, worsened by 2 new instances from ST-0005/ST-0008.
- `docs/findings/RECON-0003.md` (minor) — ST-0005's bare code-span cross-reference in `branching-policy.md`, violating `cross-reference-format.md`.

## Linter results

- Root `spec-lint`: exit 2, "spec directory not found at docs/spec" — expected/inert, root `docs/spec/` doesn't exist yet per `docs/CONTEXT-MAP.md`.
- Root `arch-lint --docs-dir docs --no-validate`: 0 errors, 0 warnings, 0 info.
- Root `backlog-lint --backlog-dir backlog`: 0 errors, 0 warnings, 0 info across 8 story files.
- Orchestrator `spec-lint --spec-dir orchestrator/docs/spec`: 0 errors, 13 warnings, 18 info (pre-existing EARS-heuristic and unreferenced-BR notices, unrelated to this range).
- Orchestrator `arch-lint --docs-dir orchestrator/docs --no-validate`: 19 errors (exactly the known pre-existing ADR-frontmatter gap, unchanged) — 0 warnings, 0 info.
- Orchestrator `backlog-lint --backlog-dir orchestrator/backlog`: 0 errors, 2 warnings across 66 story files (both on out-of-scope stories `ST-0059`/`ST-0061`, pre-existing, unrelated to this range).
