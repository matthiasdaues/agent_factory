---
id: PROP-0020
source: proposal-review
severity: minor
category: defect
artifact: docs/proposals/newcomer-onboarding-and-incremental-brownfield.md#impact
status: open
traces: []
---

# Boundary reference to non-existent reverse-map skill file

**What is wrong:** `factory/skills/reverse-map/SKILL.md` is listed in `impact.boundaries` but does not exist at the reviewed commit. The reverse-map skill is a new artifact created by this proposal (Design Section 4), not an existing file being modified. Boundary references must resolve at the reviewed commit so a reviewer can inspect the affected artifact.

**Fix:** Remove `factory/skills/reverse-map/SKILL.md` from the `impact.boundaries` list. The skill is fully described in the Design section and listed in the Scope section as a new deliverable; it does not need a boundary entry.
