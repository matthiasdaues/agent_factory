[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "usage"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/usage"]
