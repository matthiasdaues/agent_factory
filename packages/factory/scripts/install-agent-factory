#!/usr/bin/env python3
"""install-agent-factory -- bootstrap installer for Agent Factory.

Stdlib-only Python 3.8+ script. Downloads or copies a verified Factory
release into a target project.  Full implementation is a separate story;
this file exists so build-release can bundle it verbatim.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("install-agent-factory: not yet implemented", file=sys.stderr)
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
