---
owner: factory
schema: v1.schema.json
compatibility:
  policy: major-version-bump
  description: >-
    Breaking changes to the usage-record schema require a new major version
    directory (e.g. v2/). Additive, backward-compatible changes may update
    the current schema in place.
