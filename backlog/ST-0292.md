---
id: ST-0292
epic: Isolated First Task
title: Complete a first task in a repository without commits
tier: standard
status: done
deps: [ST-0291]
traces: ["scope-map#Newcomer completes one isolated task and chooses its outcome"]
touches: ["packages/factory/", "tests/factory/"]
concerns: {technical: [Factory source and packaging]}
quality-gates: []
---

# Complete a first task in a repository without commits

**Priority:** could-have

## Goal

When a repository has no HEAD, the sandbox management module creates a plain directory at `.current-work/onboarding-spike/<uuid4>/` instead of a detached worktree. The poc-spike playbook runs inside the plain sandbox, and the same three outcome paths (discard, retain, production handoff) apply.

## Contract

| Operation                              | Input state                                             | Outcome                                                  | Persistence                                                |
| -------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------- |
| Approve first-task preview             | Repository without HEAD (`git init` only), install done | Plain directory created; no `git worktree add` attempted | `.current-work/onboarding-spike/<uuid4>/` exists           |
| poc-spike playbook runs in sandbox     | Plain sandbox exists                                    | Inspectable result displayed                             | Result files inside sandbox path                           |
| Newcomer chooses discard               | Plain sandbox with completed task                       | Directory removed; path verified absent                  | `.current-work/onboarding-spike/<uuid4>/` no longer exists |
| Newcomer chooses retain with selection | Plain sandbox with completed task, artifacts selected   | Only confirmed artifacts copied; sandbox not promoted    | Selected artifacts at `docs/spikes/<name>/`                |
| Newcomer chooses production handoff    | Plain sandbox with completed task                       | Normal workstream created; sandbox not promoted          | Workstream active; sandbox does not become production work |

## Required Script Behavior

### HEAD detection

The sandbox module checks for HEAD with `git rev-parse HEAD`. When HEAD is absent (exit code 128 in a bare `git init` repository), the module creates a plain directory instead of a detached worktree.

### Plain-directory creation

The module creates `.current-work/onboarding-spike/<uuid4>/` as a regular directory using `os.makedirs`. The session-id is a UUID4 string from `uuid.uuid4()`, matching the format from ST-0291. No `git worktree add` is attempted.

### Task execution

The poc-spike playbook runs inside the plain sandbox directory. Behavior is identical to the worktree sandbox in ST-0291.

### Outcome selection

The three outcome paths (discard, retain, production handoff) reuse ST-0291's outcome logic. Discard removes the directory with `shutil.rmtree` instead of `git worktree remove`. Retain and production handoff behave identically to the worktree case.

## Boundaries

- All edits go to `packages/factory/`. Never edit `.agent-factory/factory/`.
- Outcome selection logic from ST-0291 is reused, not duplicated.
- The plain sandbox must not attempt `git worktree add` when HEAD is absent.
- The task runs only inside the sandbox directory.

## Affected Paths

- `packages/factory/` — sandbox management module (extended with plain-directory creation path).
- `tests/factory/test_onboarding_sandbox.py` — existing (from ST-0291), extended with no-HEAD tests.

## Inputs

- Feature spec: [Rule "Newcomer completes one isolated task and chooses its outcome"](../docs/spec/value-first-onboarding-journey.feature), Scenario "Repository without a commit uses a plain sandbox".
- Deps: ST-0291 delivers sandbox management module with detached-worktree creation, cleanup, retention, and handoff.
- Ownership: this story owns contract "repository-without-HEAD path" (from EPIC 5 Ownership Resolution table).

## Demo Scenario

1. Initialize a new Git repository with `git init` (no commits).
2. Install Factory (ST-0281) and complete first-session insight (ST-0287).
3. The session recommends a first task. Approve the preview.
4. A plain sandbox appears at `.current-work/onboarding-spike/<uuid4>/`. No `git worktree add` was attempted.
5. The poc-spike playbook runs inside the sandbox. An inspectable file appears.
6. Choose retain. Select one artifact. The artifact appears in `docs/spikes/<name>/`.
7. The sandbox is not promoted.

## Demo Data

None. The demo depends on a Git repository without commits (`git init` only), the installed Factory from ST-0281, the first-session insight from ST-0287, and the sandbox management module from ST-0291.

## Evidence

- [ ] `git rev-parse HEAD` returns exit code 128 in a bare `git init` repository and the module detects it (VFO-034).
- [ ] A repository without HEAD receives a plain directory at `.current-work/onboarding-spike/<uuid4>/` (VFO-034).
- [ ] No `git worktree add` is attempted when HEAD is absent.
- [ ] The poc-spike playbook produces an inspectable result inside the plain sandbox (VFO-035).
- [ ] Discard removes the plain sandbox with `shutil.rmtree` and verifies the path no longer exists (VFO-037).
- [ ] Retention copies only separately confirmed artifacts to `docs/spikes/<name>/` and removes the sandbox (VFO-036).
- [ ] Production handoff asks create-new or select-existing workstream, then delegates without promoting the sandbox (VFO-037).

## Verification

```bash
uv run pytest tests/factory/test_onboarding_sandbox.py -v --tb=short
```

## Out of Scope

- First task in a repository with commits — ST-0291.
- poc-spike playbook changes.
- Sandbox preview and approval logic — delivered by ST-0291.

## Stop Conditions

- `git rev-parse HEAD` returns unexpected results in an empty repository (e.g., shallow clones or orphan branches) — report the Git version and the observed behavior.
- The plain sandbox path conflicts with other `.current-work/` consumers — report the conflict.
- Code in `packages/factory/` and the feature spec disagree on how sandbox creation detects the absence of HEAD — report the conflicting files and the decision needed; do not add a workaround.

## Resolve Before Implementation

None — contracts are fully specified. The plain-directory path is the simpler variant of the detached-worktree path from ST-0291.

## Analysis

**What will be implemented.** `packages/factory/engine/onboarding_sandbox.py` gains a
plain-directory branch: `create_sandbox` no longer raises `SandboxError` when
`git rev-parse --verify HEAD` fails (exit 128 in a `git init`-only
repository). Instead it creates
`<repo_root>/<base_dir>/<uuid4>/` with `os.makedirs` and returns that path,
attempting no `git worktree add`. `discard_sandbox` gains a detection step —
a worktree sandbox has a `.git` file at its root (written by
`git worktree add`); a plain sandbox does not. When absent, discard uses
`shutil.rmtree` instead of `git worktree remove --force`, then verifies
absence exactly as before. `retain_artifacts` and
`request_production_handoff` need no changes: both already operate on
`sandbox_path` as an opaque directory and both already delegate final
cleanup to `discard_sandbox`, so the plain-sandbox path flows through them
unmodified — satisfying "reused, not duplicated."

**Files.**

- `packages/factory/engine/onboarding_sandbox.py` — modify `create_sandbox`
  and `discard_sandbox`; update the module docstring (the "not implemented
  here" note for the no-commit path becomes stale).
- `tests/factory/test_onboarding_sandbox.py` — remove
  `test_raises_when_repository_has_no_head` (behavior it asserts is being
  replaced), add a no-HEAD fixture and test classes for plain-sandbox
  creation, discard, retention, and production handoff; update the file's
  ownership docstring header (VFO-08-IT-01's "no commit" companion path
  moves from "out of scope here" to in-scope).

**`.feature` Scenario covered.** Rule "Newcomer completes one isolated task
and chooses its outcome", Scenario "Repository without a commit uses a
plain sandbox" (`docs/spec/value-first-onboarding-journey.feature:271`).
The discard/retain/handoff scenarios in the same Rule are already covered
by ST-0291's tests for the worktree path; this story extends their
coverage to the plain-sandbox path per the Contract table.

**Concerns/ambiguities.** None blocking. One doc note: `virgil.md`'s
"## First task" section (lines 135–175) describes only the detached-worktree
path ("creates a detached worktree from HEAD"); it does not mention the
no-HEAD branch. The story's Evidence and Contract don't require a
Virgil-doc update, and the existing `TestFirstTaskPreviewDocumented` suite
in `test_onboarding_sandbox.py` only asserts content for the worktree path,
so no test forces this. Flagging as a spec-feedback candidate for step 5
rather than blocking implementation on it.
