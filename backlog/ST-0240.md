---
id: ST-0240
epic: Validate usage records against the published Factory contract
title: Define the usage-record contract and scaffold the usage package with linter layer binding
tier: standard
status: pending
risk_level: low
deps: []
traces: [local-usage-processing-and-analysis.feature#Factory producer publishes a compatible record contract without depending on analysis, ADR-0015]
touches: [packages/, docs/]
concerns: {technical: [Python toolchain]}
quality-gates: []
---

# Define the usage-record contract and scaffold the usage package with linter layer binding

Establish the Factory-owned record contract and the Usage Analysis subproject so downstream stories have a schema to validate against and a package to extend.

**Priority:** must-have

## Goal

After this story ships, the operator inspects `packages/factory/contracts/usage-record/contract.yaml` declaring Factory as owner with a compatibility policy, `v1.schema.json` declaring a JSON Schema Draft 2020-12 record schema covering all field types and cross-field invariants, a buildable `packages/usage/` subproject, and a `deterministic_linter` entry in `docs/testing.yaml`.

## Domain Rule

- Factory owns the usage-record contract. Usage Analysis reads only the installed contract copy.
- The schema declares JSON Schema Draft 2020-12.
- `normalized_total` equals `normalized_input` plus `normalized_output` for every valid record.
- The supported CLI set is exactly `claude-code`, `pi`, `codex`, `copilot` — a closed enumeration.
- Each fixture is synthetic and contains no copied transcript or prompt content.

## Demo Scenario

1. Open `packages/factory/contracts/usage-record/contract.yaml`. Verify it names Factory as owner and declares a compatibility policy.
2. Open `packages/factory/contracts/usage-record/v1.schema.json`. Verify it declares JSON Schema Draft 2020-12 with field names, types, nullability, identifiers, timestamps, non-negative counters, nested transcript reference shape, and a `normalized_total` cross-field annotation.
3. Run `uv run --project packages/usage python -c "import usage"` and verify the package loads without error.
4. Open `docs/testing.yaml`. Verify a `deterministic_linter` layer binding exists referencing the usage subproject gate commands.

## Affected Paths

- `packages/factory/contracts/usage-record/contract.yaml` — new file
- `packages/factory/contracts/usage-record/v1.schema.json` — new file
- `packages/usage/pyproject.toml` — new file
- `packages/usage/src/usage/__init__.py` — new file
- `docs/testing.yaml` — existing file, add `deterministic_linter` layer binding

## Inputs

- ADR-0015: `docs/adr/0015-query-authoritative-jsonl-with-ephemeral-duckdb-views.md` — ephemeral DuckDB decision, contract boundary
- Architecture §5.7: `docs/arc42/05_building_block_view.md` — Usage Record Contract and Usage Analysis Runtime components
- Feature spec: `docs/spec/local-usage-processing-and-analysis.feature` — Rule: Factory producer publishes a compatible record contract
- QA strategy: `docs/spec/local-usage-processing-and-analysis-qa-strategy.md` — contract owner LU-01
- Existing testing config: `docs/testing.yaml` — layer bindings to extend
- Existing capture script: `packages/factory/scripts/usage-capture` — produces the records this schema describes
- Ownership: contract LU-01 (record schema, cross-field invariants, version agreement) is owned by this story

## Outputs

- `packages/factory/contracts/usage-record/contract.yaml` — YAML declaring Factory as contract owner, naming the schema file, and stating the compatibility policy (major version bump for breaking changes).
- `packages/factory/contracts/usage-record/v1.schema.json` — JSON Schema Draft 2020-12 declaring field names, types, nullability, identifiers (CLI, session ID, run ID, parent run ID), ISO 8601 timestamps, non-negative integer counters (normalized_input, normalized_output, normalized_total), nested transcript reference shape, and a cross-field invariant annotation.
- `packages/usage/pyproject.toml` — Python package with project metadata, Python >=3.10, no DuckDB or PyArrow dependency.
- `packages/usage/src/usage/__init__.py` — package entry point.
- `docs/testing.yaml` gains a `deterministic_linter` layer binding with the entry point for the usage contract check gate.

## Constraints

- Factory owns the contract. Usage Analysis reads only the installed copy, never the source.
- The schema uses only JSON Schema Draft 2020-12 keywords. No custom extensions.
- No DuckDB or PyArrow dependency in this story.
- The cross-field invariant is declared in the schema. Runtime enforcement is ST-0241.
- Do not modify `packages/factory/scripts/usage-capture` or any existing capture behavior.

## Suggested Agent Plan

1. Create `packages/factory/contracts/usage-record/contract.yaml` with owner declaration and compatibility policy.
2. Create `packages/factory/contracts/usage-record/v1.schema.json` with the full record schema including all field types, nested transcript reference shape, and cross-field invariant annotation.
3. Create `packages/usage/pyproject.toml` with project metadata and Python >=3.10.
4. Create `packages/usage/src/usage/__init__.py` as the package entry point.
5. Add the `deterministic_linter` layer binding to `docs/testing.yaml` referencing `packages/usage/scripts/usage-contract-check`.
6. Run `uv run --project packages/usage python -c "import usage"` to verify the package loads.

## Acceptance Criteria

- [ ] `contract.yaml` names Factory as owner and declares a compatibility policy (LU-01)
- [ ] `v1.schema.json` is valid JSON Schema Draft 2020-12 (LU-01)
- [ ] The schema declares field names, types, nullability, identifiers, timestamps, and non-negative counters (LU-01)
- [ ] The schema declares the nested transcript reference shape (LU-01)
- [ ] The schema annotates the `normalized_total = normalized_input + normalized_output` invariant (LU-01)
- [ ] `packages/usage/` is a buildable Python package with `pyproject.toml` and Python >=3.10 (LU-01)
- [ ] `docs/testing.yaml` contains a `deterministic_linter` layer binding referencing the usage subproject (LU-01)

## Verification

**Package build:**

```bash
uv run --project packages/usage python -c "import usage"
```

**Full test suite:**

```bash
uv run pytest --tb=short --quiet tests/
```

## Out of Scope

- Runtime JSONL validation (ST-0241)
- Synthetic JSONL fixtures (ST-0241)
- Conservation rules or accounting logic (EPIC 3)
- DuckDB or PyArrow dependencies (EPIC 2+)
- `usage-query` CLI entry point (ST-0242)

## Agent Stop Conditions

- If the existing `usage-capture` script writes a record format that conflicts with the schema being defined, stop and reconcile
- If `docs/testing.yaml` has an unexpected structure that prevents adding the `deterministic_linter` layer

## Resolve Before Implementation

01. Which field names does the schema use for record identity?

    > Schema uses capture's actual field names: `record_id`, `session_id`, `parent_session_id`. The feature spec's "run ID" / "parent run ID" are analytical concepts the analysis layer derives; they are not raw record fields. Decided 2026-09-13.

02. How is the `normalized_total` cross-field invariant represented in the schema?

    > As a `description` annotation on the `normalized_total` property. JSON Schema Draft 2020-12 has no arithmetic constraint keyword. Runtime enforcement is ST-0241. No custom extension keywords. Decided 2026-09-13.

03. Does this story create `usage-contract-check`, or is the testing.yaml binding a forward reference?

    > Forward reference only. The `deterministic_linter` layer binding declares the entry point path (`packages/usage/scripts/usage-contract-check`); the script itself ships with ST-0241. Decided 2026-09-13.

04. What shape does the `deterministic_linter` layer take in `testing.yaml`?

    > Single entry point, same shape as existing layers (`tool`, `infrastructure`, `entry_point`). Later stories extend the layer as they add gates. Decided 2026-09-13.

05. What does `packages/usage/src/usage/__init__.py` contain?

    > Empty file. No version string, no exports. Later stories add real code. Decided 2026-09-13.

06. What is the `contract.yaml` shape and compatibility policy wording?

    > `owner: factory`, `schema: v1.schema.json`, `compatibility:` block with `policy: major-version-bump` and a description. No consumers list — the contract declares the producer's commitment. Decided 2026-09-13.

07. Which fields does the schema declare?

    > All 27 fields from `UsageRecord`: 26 top-level plus the nested `transcript_ref` object (`path`, `span`). The schema is the full contract. Decided 2026-09-13.

08. Is `cli` required or optional in the schema?

    > Required, with `enum: ["claude-code", "pi", "codex", "copilot"]`. Every persisted record went through the CLI entrypoint where `--cli` is required. Decided 2026-09-13.

09. Which fields are required vs. nullable?

    > Eight required: `record_id`, `project_id`, `project_name`, `normalized_input`, `normalized_output`, `normalized_total`, `recorded_at`, `cli`. The remaining 19 are nullable. Decided 2026-09-13.

10. Are nullable fields absent or present with `null`?

    > Always present. `dataclasses.asdict()` serializes every field including `None` values. All 27 fields are in `required`; optional fields are typed `["<type>", "null"]`. A missing key is a malformed record. Decided 2026-09-13.

11. What is the `packages/usage/pyproject.toml` shape?

    > Src layout (`src/usage/`) with `hatchling` build backend. `name = "usage"`, `version = "0.0.0"`, `requires-python = ">=3.10"`, no dependencies. Decided 2026-09-13.
