---
id: ST-0180
epic: VIRGIL Unified Session Agent
title: Update INDEX.yaml for VIRGIL and new skills
tier: economy
status: pending
deps: [ST-0170, ST-0171, ST-0172, ST-0179, ST-0181]
traces: [docs/proposals/newcomer-tour-as-portable-skill.md]
outputs: [factory/INDEX.yaml]
quality-gates: []
notes: "crap-score excluded: configuration file, no production code. dependency-check excluded: no imports."
---

# Update INDEX.yaml for VIRGIL and new skills

Update `factory/INDEX.yaml` to register the new VIRGIL agent and the two
new skills (newcomer-tour, explain-concept). Remove the retired chat-agent
and kit-manager entries.

**Priority:** must-have

## Acceptance Criteria

- `factory/INDEX.yaml` contains an entry for `virgil` under agents, pointing
  to `factory/agents/virgil.md`.
- `factory/INDEX.yaml` contains entries for `newcomer-tour` and
  `explain-concept` under skills, pointing to their respective SKILL.md
  files.
- The `chat-agent` entry under agents is removed.
- The `kit-manager` entry under agents is removed.
- The INDEX.yaml file parses as valid YAML.
