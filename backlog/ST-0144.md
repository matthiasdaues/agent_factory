---
id: ST-0144
epic: Brownfield-Lite
title: Create reverse-map skill
tier: strong
status: pending
deps: []
traces: [newcomer-onboarding.feature Rule 6]
outputs: [factory/skills/reverse-map/SKILL.md]
quality-gates: []
notes: "crap-score excluded: no production code. mutation-analysis excluded: no production code. dependency-check excluded: no production code. Output is a skill definition file validated by index-lint and link-check."
---

# Create reverse-map skill

Create `factory/skills/reverse-map/SKILL.md` — a forensic skill that populates `docs/spec/scope-map.md` from code, tests, and unstructured sources. The skill follows an evidence hierarchy (tests first, then code entry points, then docs/stakeholder knowledge) and presents results in batches by domain area for stakeholder confirmation.

**Priority:** must-have

## Acceptance Criteria

- `factory/skills/reverse-map/SKILL.md` exists with valid skill frontmatter
- The skill documents the forensic evidence hierarchy (verified → flagged → high → medium-high → medium → medium-low → low → lowest → claimed)
- The skill sweeps tests first, then code entry points, then accepts additional sources
- Results are presented in batches by domain area for stakeholder confirmation
- The scope-map output uses the 5-column format: Rule, Status, Confidence, Sources, Feature Link
- The skill seeds `docs/CONTEXT.md` with domain vocabulary during the code sweep
- The stakeholder can end the sweep at any time ("that's enough")
- `index-lint` regenerates INDEX.yaml including the new skill without errors
- `link-check` passes

## Implementation Notes

- Category: `requirements`
- The confidence hierarchy matches the entity model's SCOPE_MAP_ROW.confidence enum and the validation-rules confidence table
- Plain language UX — no factory jargon in stakeholder-facing output
- Domain area grouping inferred from top-level module/package boundaries, confirmed with stakeholder
