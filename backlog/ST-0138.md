---
id: ST-0138
epic: Tier-Aware Delegation
title: "Convention docs: rubric in dispatch-contract, planning-agent citation, and A/B measurement"
tier: economy
status: done
deps: [ST-0132, ST-0135]
traces:
  - "Feature: Tier Rubric"
  - "Feature: Subagent Handoff Contract"
outputs:
  - factory/rulebooks/conventions/dispatch-contract.md
  - factory/agents/planning-agent.md
  - factory/rulebooks/templates/story.md
---

# Convention docs: rubric in dispatch-contract, planning-agent citation, and A/B measurement

Record the tier rubric table in `dispatch-contract.md` and cite it from
`planning-agent.md` (one table, no copy). Update the story template
documentation for `risk_domains` and `strategy` selection guidance. Define the
A/B measurement protocol for the first wave after Phase 3 landing: hypothesis,
control, minimum sample, and decision trigger.

**As a** planning agent,
**I want** the tier rubric documented in one authoritative location and
referenced where needed,
**so that** tier suggestions are consistent and the rubric can be revised from
one source.

**Priority:** must-have

## Acceptance Criteria

1. `dispatch-contract.md` contains the tier rubric table (first-match-wins
   rules).
2. `planning-agent.md` cites the rubric in `dispatch-contract.md` without
   copying it.
3. `story.md` template includes selection guidance for `strategy`: use
   `seams-first` when acceptance criteria are expressible as test assertions
   and the implementation path is not obvious from the tests alone; use
   `direct` otherwise.
4. The A/B measurement protocol is documented: hypothesis (rubric-assigned
   tiers produce lower normalized_total and equal or fewer rework attempts),
   control (last three pre-rubric dispatches), minimum sample (8 merged
   stories), and decision trigger.

## Gherkin Scenario Traceability

From **Feature: Tier Rubric** (Bounded Context 3):

- (Rubric documentation is a normative deliverable, not a behavioral scenario)

From **Feature: Subagent Handoff Contract** (Bounded Context 3):

- (Handoff contract documentation is a normative deliverable)

## QA Strategy Reference

No additional test files. Documentation correctness is validated by inspection.

## Implementation Notes

**Inputs:**

- `docs/spec/supplementary_specs/mechanized-dispatch.md` -- Features: Tier
  Rubric, Subagent Handoff Contract
- `docs/proposals/mechanized-dispatch-and-step-isolation.md` -- Phase 3
  completion criteria: A/B measurement
- `factory/rulebooks/conventions/dispatch-contract.md` -- existing document
- `factory/agents/planning-agent.md` -- existing agent definition

**Strategy:** direct.

**Tier rationale:** economy. Documentation edits only.

## Analysis

- Update `factory/rulebooks/conventions/dispatch-contract.md` with the authoritative tier rubric table, ordered first-match-wins, so planning and dispatch share one source of truth.
- Update `factory/agents/planning-agent.md` to cite the rubric table instead of copying it.
- Update `factory/rulebooks/templates/story.md` to clarify when to use `risk_domains` and how to choose `strategy` (`seams-first` vs `direct`).
- Use cases covered: Tier Rubric and Subagent Handoff Contract (normative documentation only). The A/B measurement protocol is already defined in the accepted proposal, so no additional file change is needed for it.
- Concern: keep the rubric table singular and referenced, not duplicated, so future changes only touch one authoritative source.
