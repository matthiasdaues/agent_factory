---
id: ST-0290
epic: Value-First Session
title: Configure hooks grouped by protected outcome with source-only triggers filtered
tier: standard
status: pending
deps: [ST-0289]
traces: ["scope-map#Newcomer sees gate value before choosing hooks"]
touches: ["packages/factory/config/", "packages/factory/agents/", "tests/factory/"]
concerns: {technical: [Factory source and packaging]}
quality-gates: []
---

# Configure hooks grouped by protected outcome with source-only triggers filtered

**Priority:** must-have

## Goal

When fitting introduces hook configuration, the session presents hooks grouped by protected outcome and asks only about automatic file changes or material execution cost. Unavailable hooks appear with a description, not as errors or choices. The consumer hook set omits `index-lint` and any hook that matches only ignored Factory runtime paths (BUG-0029). `matrix-lint` runs after model configuration and before dispatch.

## Contract

| Operation                                                             | Input state                           | Outcome                                                                                                          | Persistence                                           |
| --------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| Fitting presents hook configuration                                   | Gate demonstration complete (ST-0289) | Hooks grouped by protected outcome (e.g., "Formatting: mdformat", "Dangerous git commands: block-dangerous-git") | No configuration written yet                          |
| Hook performs automatic file changes                                  | Hook listed in group                  | Session asks for consent                                                                                         | Configuration written on approval                     |
| Hook carries no automatic file changes and no material execution cost | Hook listed in group                  | Hook auto-included without asking                                                                                | Configuration includes the hook                       |
| Hook unavailable for detected project                                 | Hook exists in Factory set            | Hook appears with a description, not as an error or choice                                                       | Hook excluded from configuration                      |
| `index-lint`                                                          | Consumer hook subset generated        | `index-lint` absent from consumer set                                                                            | `index-lint` not in generated config                  |
| Hook matches only ignored Factory runtime paths                       | Consumer hook subset generated        | Hook excluded from consumer set (BUG-0029)                                                                       | Hook not in generated config                          |
| `matrix-lint`                                                         | Model configuration complete          | `matrix-lint` scheduled after model configuration and before dispatch                                            | `matrix-lint` in generated config at correct position |

## Required Script Behavior

### Hook metadata

A companion file at `packages/factory/config/hook-metadata.yaml` maps each hook id to:

- **outcome**: the protected outcome group (e.g. "Formatting", "Dangerous git commands", "Specification consistency").
- **trade_off**: `auto-fix` (modifies files), `material-cost` (slow or resource-heavy), or `none`.
- **availability**: conditions under which the hook is relevant (e.g. detected language, presence of spec files).

### Consumer subset generation

The filter reads `pre-commit-config.yaml` and `hook-metadata.yaml`, then:

1. Excludes `index-lint` unconditionally.
2. Excludes hooks whose `files` regex matches only Factory-internal paths (`.agent-factory/factory/`), tested against `git ls-files` output (BUG-0029).
3. Excludes hooks whose availability conditions are not met by the detected project state.
4. Groups remaining hooks by protected outcome.

### Consent logic

- Hooks with `trade_off: none` are auto-included without asking.
- Hooks with `trade_off: auto-fix` or `trade_off: material-cost` require separate consent per hook.
- Unavailable hooks appear with a description. They are not errors or choices.

### matrix-lint ordering

`matrix-lint` runs after model configuration is complete and before dispatch. The generated config places it in the correct hook order.

## Boundaries

- Do not remove hooks from `pre-commit-config.yaml` — filter them at subset generation time.
- Do not rewrite `merge-precommit-config` from scratch — extend or wrap the existing script.
- The hook presentation does not default to selecting all hooks. Each hook that performs automatic file changes or carries material cost requires separate consent.
- All edits go to `packages/factory/`. Never edit `.agent-factory/factory/`.

## Affected Paths

- `packages/factory/config/pre-commit-config.yaml` — existing (116 lines), read to derive consumer subset.
- `packages/factory/agents/virgil.md` — existing, modified to add outcome-grouped hook presentation flow.
- `packages/factory/scripts/` — new or modified hook-subset generation logic.
- `tests/factory/test_merge_precommit_config.py` — existing test file for merge-precommit-config.
- `tests/factory/` — new `test_precommit_config_contract.py`.

## Inputs

- ST-0289 deliverables: gate demonstration complete, Virgil session active.
- `packages/factory/config/pre-commit-config.yaml` (116 lines) — full hook definitions with repo, rev, and hook entries.
- `packages/factory/scripts/merge-precommit-config` — existing script that merges Factory hooks into project config.
- `tests/factory/test_merge_precommit_config.py` — existing tests for merge logic.
- `packages/factory/agents/virgil.md` — existing session agent definition.
- Feature spec: [Rule "Newcomer sees gate value before choosing hooks"](../docs/spec/value-first-onboarding-journey.feature) — scenarios "Hook choices describe protected outcomes and material trade-offs" and "Consumer hook set omits source-only triggers".
- Ownership: this story owns contracts "outcome-based hook selection" and "filtered consumer hook set" (from EPIC 4 Ownership Resolution table).

## Demo Scenario

1. Open a Factory session on a project where hooks have not been configured.
2. Complete the gate demonstration (ST-0289).
3. Fitting presents hooks grouped by protected outcome: e.g., "Formatting: mdformat", "Dangerous git commands: block-dangerous-git".
4. The session asks about mdformat (automatic file changes). Approve.
5. The session does not ask about block-dangerous-git (no automatic file changes, no material execution cost).
6. An unavailable hook (e.g., a linter for a language not detected in the project) appears with a description, not as an error.
7. Verify `index-lint` is absent from the consumer hook set.
8. Verify no hook in the generated consumer set matches only paths under `.agent-factory/factory/`.
9. Configure the model matrix. Verify `matrix-lint` runs after model configuration and before dispatch.

## Demo Data

None. The demo depends on the installed Factory hooks from `packages/factory/config/pre-commit-config.yaml` and the project state from ST-0281 (installation) and ST-0287 (first-session insight).

## Evidence

- [ ] Hooks are grouped by protected outcome in the session presentation (VFO-031).
- [ ] The session asks only about hooks that perform automatic file changes or carry material execution cost (VFO-031).
- [ ] Unavailable hooks appear with a description, not as errors or choices (VFO-031).
- [ ] `index-lint` is absent from the consumer hook set (VFO-031).
- [ ] No consumer hook matches only ignored Factory runtime paths (VFO-031, BUG-0029).
- [ ] `matrix-lint` runs after model configuration and before dispatch (VFO-032).

## Verification

**Contract tests:**

```bash
uv run pytest tests/factory/test_precommit_config_contract.py -v --tb=short
```

**Existing merge tests (regression):**

```bash
uv run pytest tests/factory/test_merge_precommit_config.py -v --tb=short
```

## Out of Scope

- Rewriting `merge-precommit-config` from scratch.
- Adding new hooks to `pre-commit-config.yaml`.
- Model matrix configuration logic (exists today, deferred by the new flow).
- Gate demonstration — delivered by ST-0289.

## Stop Conditions

- `merge-precommit-config` has undocumented side effects that conflict with subset filtering → report the conflict.
- Code in `packages/factory/config/pre-commit-config.yaml` and the feature spec disagree on which hooks belong in the consumer set → report the conflicting files and the decision needed; do not add a workaround.

## Resolve Before Implementation

1. What data structure holds the mapping from hook id to protected outcome and trade-off category — a YAML companion file, inline comments in `pre-commit-config.yaml`, or a Python dictionary in the filter script?

   > A YAML companion file at `packages/factory/config/hook-metadata.yaml`. Separates presentation metadata from hook definitions. Each entry maps a hook id to its protected outcome group, trade-off category (`auto-fix`, `material-cost`, or `none`), and availability conditions. Readable independently by the filter script and tests. Decided 2026-09-24.

2. How does the filter determine whether a hook matches "only" gitignored paths — by comparing the hook's `files` regex against `.gitignore` patterns statically, or by testing against a representative file list?

   > Test against a representative file list. The filter runs the hook's `files` regex against the file list from `git ls-files` in the target project. If every match is under a Factory-internal path (`.agent-factory/factory/`), the hook is excluded from the consumer set. This is cheaper and more accurate than static regex-vs-gitignore comparison. Decided 2026-09-24.
