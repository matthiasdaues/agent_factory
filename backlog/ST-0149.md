---
id: ST-0149
epic: Test Gate Infrastructure
title: Remove test hook from pre-commit config
tier: economy
status: pending
deps: [ST-0148]
traces: [UC-09]
outputs: [factory/config/pre-commit-config.yaml]
quality-gates: []
notes: "crap-score excluded: config file edit, no production code. mutation-analysis excluded: no production code. dependency-check excluded: no imports."
---

# Remove test hook from pre-commit config

Remove the `agent_factory_hook-run-tests-full` entry from `factory/config/pre-commit-config.yaml` (lines 93-100). Factory stops injecting any test-related hook into `.pre-commit-config.yaml`. Testing hooks are project-owned.

**Priority:** must-have

## Acceptance Criteria

- `factory/config/pre-commit-config.yaml` contains no entry named `agent_factory_hook-run-tests-full`
- No Factory-owned test hook is injected into consumer projects
- All other pre-commit hooks (mdformat, ruff, spec-lint, arch-lint, transition-lint, etc.) remain functional
- Projects with their own pre-commit test hooks are unaffected

## Implementation Notes

- Remove only the `agent_factory_hook-run-tests-full` entry; leave all structural hooks intact
- The entry is at lines 93-100 of the current file
