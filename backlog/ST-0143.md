---
id: ST-0143
epic: Session Entrypoint
title: Revise session entrypoint to four-option menu
tier: standard
status: done
deps: [ST-0140, ST-0142]
traces: [newcomer-onboarding.feature Rule 3, newcomer-onboarding.feature Rule 1]
outputs: [factory/config/AGENTS.md]
risk_domains: [compatibility]
quality-gates: []
notes: "crap-score excluded: no production code. mutation-analysis excluded: no production code. dependency-check excluded: no production code. Output is a config file validated by link-check and mdformat."
---

# Revise session entrypoint to four-option menu

Replace the current A/B/C session entrypoint in `factory/config/AGENTS.md` with A/B/C/D:

- A — I'm new here — show me around (guided tour)
- B — I want to start something (former option A content, unchanged routing)
- C — I want to run an agent or playbook directly (former option B)
- D — I just want to talk something through (adopt chat-agent)

**Priority:** must-have

## Acceptance Criteria

- Option A reads `docs/arc42/beginner-intro.md` and walks the user through it conversationally
- Option A checks for prior work (completed poc-spike, charter) before starting
- Option B contains the same intention tree as the former option A with unchanged playbook routing
- Option C contains the same content as the former option B
- Option D adopts the chat-agent role via the adopt pattern (not spawning a subagent)
- All CLI orientation symlinks (`.claude/CLAUDE.md`, etc.) inherit the change via the existing symlink chain
- `link-check` and `mdformat` pass

## Implementation Notes

- AGENTS.md is the canonical source; CLI-specific files symlink to it
- The existing option A intention tree moves to option B — all playbook routing preserved
- The existing option B factory-content listing moves to option C
- Option D replaces the current option C "Let's talk" section
