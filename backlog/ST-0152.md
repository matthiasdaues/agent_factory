---
id: ST-0152
epic: Test Gate Infrastructure
title: Update implementation-agent for two-gate default
tier: economy
status: pending
deps: [ST-0147]
traces: [ADR-0012]
outputs: [factory/agents/implementation-agent.md]
quality-gates: []
notes: "crap-score excluded: agent definition file, no production code. mutation-analysis excluded: no production code. dependency-check excluded: no imports."
---

# Update implementation-agent for two-gate default

Update `factory/agents/implementation-agent.md` to reflect the two-gate dispatcher sequence. Remove `factory/scripts/mutation-analysis` from `inputs:` (line 28), change the hardcoded three-gate default to two gates (`crap-score`, `dependency-check`) (line 129), and remove the `mutation-analysis` CLI invocation from the gate execution section (lines 136-139).

**Priority:** must-have

## Acceptance Criteria

- `factory/agents/implementation-agent.md` does not list `factory/scripts/mutation-analysis` in `inputs:`
- The hardcoded gate default is two gates: `crap-score`, `dependency-check`
- No `mutation-analysis` CLI invocation appears in the gate execution section
- The agent definition is internally consistent with ADR-0012's amended two-gate sequence
- `mdformat` passes on the modified file

## Implementation Notes

- Three specific line ranges to update: line 28 (inputs), line 129 (gate default), lines 136-139 (CLI invocation)
- The dispatcher's quality sequence changes from `crap-score, mutation-analysis, dependency-check` to `crap-score, dependency-check`
