---
id: ST-0083
epic: Project Charter
title: Update greenfield playbook with charter workflow
tier: standard
status: done
deps: [ST-0077, ST-0080, ST-0082]
traces: []
outputs: [factory/playbooks/greenfield-development.md]
---

# Update greenfield playbook with charter workflow

Insert charter steps into the greenfield development playbook: scaffold after vision, update during requirements and architecture, completeness sweep before planning, planning gate enforcement.

**Priority:** must-have

## Acceptance Criteria

- New Step 1.0 (before current Step 1.1): `capture-charter --init` — scaffold skeleton, fill what is known from vision
- During requirements and architecture phases: agents invoke `update-charter` as decisions emerge (documented in the phase descriptions)
- After architecture review passes: `capture-charter` completeness sweep — fill gaps, derive Epic 0
- Before planning: planning gate checks `charter-lint --planning-gate` + stakeholder approval of charter and Epic 0 together
- Planning step acknowledges Epic 0 stories exist in backlog and derives feature stories after them
- Implementation wave 1 is Epic 0 — no feature stories until wave 1 complete
- Phase Boundary Contract table updated if new transitions are introduced

## Implementation Notes

The planning gate follows the same approval pattern as backlog approval in Step 3.3. The charter completeness sweep and planning gate are between architecture review and planning — a new decision point in the flow.

## Analysis

Documentation-only story. Edited `factory/playbooks/greenfield-development.md` (deps ST-0077, ST-0080, ST-0082 confirmed `done` — `capture-charter` skill, charter-aware `planning-agent`/`create-backlog`, and charter-aware `implementation-agent` already exist):

- New Step 1.0 before Step 1.1: `capture-charter --init`, right after vision capture.
- Step 1.1 and Step 2.1 note `update-charter` invocation as requirements/architecture decisions settle charter entries.
- New Step 2.5 (charter completeness sweep, `capture-charter` no-flag) and Step 2.6 (planning gate, `charter-lint --planning-gate` + stakeholder approval of charter and Epic 0 together) inserted between Decision Point 2.3's clean path and Phase 3; Decision Point 2.3 now routes to Step 2.5 instead of straight to Phase 3.
- Step 3.1 notes planning-agent acknowledges existing Epic 0 stories and chains feature-story `deps:` after them.
- Step 4.1 notes wave 1 is Epic 0, enforced via the `deps:` chain (no new scheduling logic).
- Phase Boundary Contract row `architecture-review-agent → planning-agent` route condition updated to include the charter gate; no new transition row added since `capture-charter` runs in the orchestrating session, not as a spawned agent.
- Version bumped 1.1.0 → 1.2.0. Formatted with `factory/scripts/mdformat --number`.

**Known drift not fixed here (out of story's declared `outputs:`)**: `factory/playbooks/greenfield-development.fsm.yml` is derived from this playbook and consumed by the `run-step` skill, but has no states for the new Step 1.0/2.5/2.6. Flagged to `charter-dispatch` as a follow-up rather than expanded in-scope, to avoid touching a file outside this story's declared outputs during a parallel dispatch.

All acceptance criteria verified against the edited playbook.
