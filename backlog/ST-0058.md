---
id: ST-0058
epic: Codex CLI Integration
title: Remove the complete Codex Factory footprint tracelessly
status: done
tier: strong
deps: [ST-0057]
traces: []
outputs: [factory/scripts/remove-factory, orchestrator/tests/test_remove_factory.py, orchestrator/tests/test_init_factory_codex.py]
---

# Remove the complete Codex Factory footprint tracelessly

Extend the install manifest and remover to reverse skills, generated agents,
aliases, guardrail, and capture configuration while preserving project-owned
Codex content.

**Priority:** must-have

## Analysis

Removal observes behavior through the `remove-factory` CLI boundary. The
implementation records enough hook ownership state during installation to
restore an untouched project hook file byte-for-byte, or to remove only the
recorded Factory handlers after later project edits. Manifest paths and
symlinked ancestors are validated before mutation; generated agents and Codex
links require current ownership evidence before deletion. Shared `.agents` and
`.codex` directories are pruned deepest-first only when empty.

## Acceptance Criteria

- The manifest records every Factory-owned `.agents` and `.codex` path plus
  whether `.codex/hooks.json` pre-existed.
- Removal strips exact Factory hook handlers from project-owned JSON, including
  guardrail and capture entries, and deletes a Factory-created hook file.
- Generated agents and linked skills/aliases are removed without following or
  deleting foreign paths.
- `.agents`, `.agents/skills`, `.codex`, and `.codex/hooks` are pruned only when
  empty.
- Fresh install/remove and project-content coexistence round trips are
  byte-identical.
