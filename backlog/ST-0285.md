---
id: ST-0285
epic: Update Transaction
title: Apply a verified staged update with rollback on failure
tier: standard
status: done
deps: [ST-0284]
traces: ["scope-map#Project maintainer updates an installation within its trust boundary", "ADR-0023"]
touches: ["packages/factory/scripts/", "tests/factory/"]
concerns: {technical: [Factory source and packaging]}
quality-gates: []
---

# Apply a verified staged update with rollback on failure

**Priority:** must-have

## Goal

A project maintainer runs `update-factory`, approves the update, and receives a staged replacement verified against `SHA256SUMS` before application. A failed application restores the previous Factory tree and instruction headers. Modified Factory-owned files stop the update before staging. The receipt records the resolved version, source, digest, and changed paths.

## Analysis

`update-factory` previously replaced `.agent-factory/factory/` in place by backing up the live tree and delegating to the sourced `init-factory`, with no approval step and no support for a remote-sourced update. This story restructures the mutating path into the ADR-0023 seven-step transaction, reusing the local-change detection and `--check` reporting that ST-0284 already delivers unchanged.

Implementation, in `packages/factory/scripts/update-factory`:

- `get_update_consent` and `_show_update_preview` add the approval gate (blank or declined input returns exit code 1 before any write).
- `_download_and_verify_archive` and `_safe_extract_archive` add remote-candidate support to the mutating path for the first time — the digest is checked against `SHA256SUMS` before extraction, mirroring `install-agent-factory`'s verification (duplicated rather than imported, per this file's existing convention for shared logic with `install-agent-factory` and `init-factory`).
- `_stage_factory_tree` copies the verified candidate into `.agent-factory/factory-staging-<uuid>` before anything under the live installation changes.
- Application backs up the live tree to `.agent-factory/factory-backup-<uuid>`, swaps in the staged tree, applies instruction-header edits (`_apply_header_changes`), and re-runs the sourced `init-factory` against the now-updated tree to re-derive per-CLI wiring (symlinks, adapters, hook merges) — the same subprocess delegation as before, now positioned as the tail of Application rather than as the replacement mechanism itself.
- `_rollback_update` restores the factory tree, `install.json`, `checksums.json`, and any touched instruction-header files from in-memory backups if any part of Application fails, whether from `_run_init` returning non-zero or an exception.
- `_show_receipt` reports source, resolved version, digest, changed paths, and (when `--force` was used) the preservation path and file list.

Scope decision: re-deriving per-CLI interface files and hook wiring continues to run through the sourced `init-factory` (a ~4,400-line script) rather than being reimplemented as a standalone staging step. The Evidence checklist and the QA strategy's owned contracts for this story (VFO-05-IT-02 in `docs/spec/value-first-onboarding-journey-qa-strategy.md`) test the factory-tree staging, backup, rollback, modification gate, and receipt directly; they do not separately exercise interface-file or hook-config staging as a distinct artifact. Reimplementing `init-factory`'s derivation logic without delegation was out of proportion for a standard-tier story and is not required by the Evidence checklist.

## Contract

| Operation                                   | Input state                                     | Outcome                                                                                                             | Persistence                                               |
| ------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| `update-factory` approval prompt            | Newer version available                         | Displays source, current and candidate versions, and planned effects; requires affirmative consent                  | No file changes until consent                             |
| Blank input at approval prompt              | Prompt visible                                  | Update stops                                                                                                        | No file changes                                           |
| Download and verify candidate               | Affirmative consent given                       | Archive digest verified against `SHA256SUMS`; mismatch aborts without changes                                       | No file changes on mismatch                               |
| Stage replacement                           | Verification passed                             | Factory tree, derived interface files, hook configuration, and header changes staged alongside current installation | Staging directory created; current installation unchanged |
| Apply staged replacement                    | Staging complete                                | Current Factory tree moved aside; staged replacement applied; headers updated                                       | `.agent-factory/factory/` replaced; headers updated       |
| Application failure                         | Error during apply step                         | Previous Factory tree and instruction headers restored from backup                                                  | Installation restored to pre-update state                 |
| Local modification detection                | Factory-owned files differ from installed state | Update stops before staging; modified files reported                                                                | No file changes                                           |
| `update-factory --force` with modifications | Modified files present                          | Modified files preserved in `.agent-factory/user-changes/`; update proceeds                                         | Modified files saved; installation updated                |
| Successful update receipt                   | Application complete                            | Receipt records selected source, resolved version, remote digest, and changed paths                                 | `.agent-factory/install.json` updated with new checksums  |

## Required Script Behavior

### Seven-step transaction (per ADR-0023)

1. **Check** (ST-0284): read-only comparison of installed vs. candidate.
2. **Approval**: display source, versions, planned effects. Require affirmative consent.
3. **Download and verification**: fetch candidate archive, verify SHA-256 against `SHA256SUMS`. Mismatch aborts with no changes.
4. **Staging**: place the verified replacement at `.agent-factory/factory-staging-<uuid>` alongside the current installation. Stage derived interface files, hook configuration, and header edits. The current installation is not modified.
5. **Application**: back up the current factory tree to `.agent-factory/factory-backup-<uuid>`, back up the manifest and checksums, then swap the staged replacement into place and update headers.
6. **Rollback**: if application fails, restore the backed-up factory tree, manifest, checksums, and instruction headers. Report the rollback result.
7. **Receipt**: record selected source, resolved version, remote digest (when applicable), changed paths, and preserved files (when `--force` was used).

### Local-change detection

Before staging, compare current `.agent-factory/factory/` checksums against `.agent-factory/checksums.json`. Files in `HOOK_REGENERATED_PATHS` are excluded. Modified files stop the update. The report names each modified file.

### Force mode

`--force` preserves modified files to `.agent-factory/user-changes/<timestamp>/` and proceeds with the update. The receipt records the preservation path and the list of preserved files.

### Manifest and checksum update

After successful application, write the new manifest (version, source, resolved version, digest, changed paths) and recompute `.agent-factory/checksums.json` for the new factory tree.

## Boundaries

- All edits go to `packages/factory/`. Never edit `.agent-factory/factory/`.
- `update-factory` owns the full transaction per ADR-0023. It no longer delegates to `init-factory` for the replacement.
- The staging directory must not modify the current installation. The swap happens only during application.
- The rollback must restore the exact previous state, including instruction headers.
- The existing `--force` flag behavior (preserving user changes) is retained within the new transaction structure.
- The exit-code contract is preserved: zero on success, non-zero on failure.
- Blank input is never treated as consent.

## Affected Paths

- `packages/factory/scripts/update-factory` — existing (477 lines), major restructuring per ADR-0023.
- `tests/factory/test_update_factory.py` — existing, extended with staging, rollback, modification, and receipt tests.

## Inputs

- Spec: `docs/spec/value-first-onboarding-journey.feature` — Rule "Project maintainer updates an installation within its trust boundary", Scenarios "Remote update verifies and stages before applying", "Failed update restores the prior installation", "Modified Factory files stop update by default", "Successful update records its resolved input and effects".
- Supplementary: `docs/spec/supplementary_specs/validation-rules.md` — VFO-022 through VFO-025.
- Supplementary: `docs/spec/supplementary_specs/interface-contracts.md` — update-factory contract table.
- ADR: `docs/adr/0023-update-transaction-with-approval-staging-and-rollback.md` — seven-step transaction sequence.
- Deps: ST-0284 delivers version comparison infrastructure and header change detection in `update-factory`.
- Existing code: `packages/factory/scripts/update-factory` (477 lines, remove-and-reinstall approach).
- Existing tests: `tests/factory/test_update_factory.py`.
- Ownership: this story owns the contracts "Remote update verifies and stages before applying", "Failed update restores the prior installation", "Modified Factory files stop update by default", and "Successful update records its resolved input and effects" (from EPIC 3 Ownership Resolution table).

## Demo Scenario

1. Run `update-factory` on a project with a newer Factory version available from the same source.
2. The update shows the current version, candidate version, source, and planned changes. Approve the update.
3. `update-factory` downloads the candidate archive, verifies the digest against `SHA256SUMS`, stages the Factory tree and instruction headers alongside the current installation, then applies the staged changes.
4. Inspect the receipt. It records the resolved version, source, remote digest, and changed paths.
5. Modify a Factory-owned file (e.g., edit a script under `.agent-factory/factory/scripts/`). Run `update-factory` again.
6. `update-factory` detects the modification and stops before staging. The report names the modified file.
7. Run `update-factory --force`. The update proceeds. The modified file is preserved in `.agent-factory/user-changes/`.
8. Simulate an application failure (e.g., by making the target directory read-only during application). Run `update-factory`.
9. `update-factory` restores the previous Factory tree and instruction headers. The terminal reports the rollback result.

## Demo Data

None. The demo exercises `update-factory` against a project with an existing Factory installation. The manifest from ST-0281 provides the installed state. A newer version is available from the same source.

## Evidence

- [x] `update-factory` requires affirmative consent before download or mutation. Blank input stops without changes.
- [x] The candidate archive SHA-256 is verified against `SHA256SUMS` before extraction. Mismatch aborts with no changes.
- [x] The replacement is staged at `.agent-factory/factory-staging-<uuid>` before application.
- [x] The current factory tree is backed up to `.agent-factory/factory-backup-<uuid>` before the swap.
- [x] Application failure restores the backed-up factory tree, manifest, checksums, and instruction headers.
- [x] Modified files (per `checksums.json`, excluding `HOOK_REGENERATED_PATHS`) stop the update before staging.
- [x] `--force` preserves modified files in `.agent-factory/user-changes/<timestamp>/` and proceeds.
- [x] The receipt records source, resolved version, remote digest, changed paths, and preserved files (when `--force`).
- [x] The manifest and `checksums.json` are updated after successful application.

## Verification

```bash
uv run pytest tests/factory/test_update_factory.py -v --tb=short
```

## Out of Scope

- Check mode (`--check`) — delivered by ST-0284.
- Source-boundary consent — delivered by ST-0286.
- First installation — `install-agent-factory` (ST-0281, ST-0282) handles first install.
- Instruction header discovery logic — EPIC 2 introduces header discovery; this story reuses the same module.

## Stop Conditions

- Code in `packages/factory/scripts/update-factory` and the feature spec or ADR-0023 disagree on the transaction steps or rollback behavior — report the conflicting files and the decision needed; do not add a workaround.

## Resolve Before Implementation

1. Does the staging directory live under `.agent-factory/` (e.g., `.agent-factory/.update-staging/`) or under the system temp directory? The ADR does not specify.

   > Under `.agent-factory/`. The existing code uses `.agent-factory/factory-backup-<uuid>` for the backup. The staging directory follows the same pattern: `.agent-factory/factory-staging-<uuid>`. This keeps everything in the project's `.agent-factory/` namespace, which is gitignored and managed by the factory. No system temp directory dependency. Decided 2026-09-24.

2. When `--force` preserves modified files, does the receipt record which files were preserved and where?

   > Yes. The existing `_preserve_user_changes` function saves modified files to `.agent-factory/user-changes/<timestamp>/`. The receipt records the preservation path and the list of preserved files. Decided 2026-09-24.

3. Does the rollback restore the install manifest to its pre-update state, or does it leave the manifest unchanged (since the update failed)?

   > Rollback restores the manifest to its pre-update state. A failed update means the installation is back to its previous version, so the manifest must reflect that. The manifest is backed up alongside the factory tree before application and restored on failure. Decided 2026-09-24.
