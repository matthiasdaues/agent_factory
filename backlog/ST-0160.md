---
id: ST-0160
epic: arc42 Projection Gating
title: Backfill arc42.projected property in existing DSL files
tier: simple
status: pending
deps: [ST-0159]
traces: []
outputs: [docs/arc42/architecture.dsl, orchestrator/docs/architecture.dsl, factory/fixtures/quality-gates/dependency-violation/architecture.dsl]
quality-gates: [arch-lint]
notes: ""
---

# Backfill arc42.projected property in existing DSL files

Add a workspace-level `properties` block with `arc42.projected` to all three existing DSL files:

| File                                                                   | Value     | Reason                               |
| ---------------------------------------------------------------------- | --------- | ------------------------------------ |
| `docs/arc42/architecture.dsl`                                          | `"true"`  | Full arc42 chapters and images exist |
| `orchestrator/docs/architecture.dsl`                                   | `"true"`  | Has ch5, ch6, ch9, and images        |
| `factory/fixtures/quality-gates/dependency-violation/architecture.dsl` | `"false"` | Minimal fixture, no arc42 chapters   |

Insert the `properties` block between the `workspace` declaration and `model {`.

**Priority:** must-have

## Acceptance Criteria

- Each DSL file has a workspace-level `properties` block with `"arc42.projected"` set to the correct value
- `arch-lint --docs-dir docs/arc42` passes without errors
- `arch-lint --docs-dir orchestrator/docs` passes without errors
- DSL files remain valid Structurizr syntax

## Implementation Notes

- Worktree copies under `.current-work/` are not touched; they merge forward naturally
