---
id: ST-0179
epic: VIRGIL Unified Session Agent
title: Retire chat-agent and kit-manager agent files
tier: economy
status: pending
deps: [ST-0170, ST-0173, ST-0174, ST-0177, ST-0178]
traces: [docs/proposals/newcomer-tour-as-portable-skill.md]
outputs: [factory/agents/chat-agent.md, factory/agents/kit-manager.md]
strategy: deletion
quality-gates: []
notes: "crap-score excluded: file deletion, no production code. dependency-check excluded: no imports. mutation-analysis excluded: no production code."
---

# Retire chat-agent and kit-manager agent files

Delete `factory/agents/chat-agent.md` and `factory/agents/kit-manager.md`.
These agents are superseded by VIRGIL. All factory-shipped references must
be updated before deletion (enforced via dependencies on ST-0173, ST-0174,
ST-0177, ST-0178).

**Priority:** must-have

## Acceptance Criteria

- `factory/agents/chat-agent.md` no longer exists.
- `factory/agents/kit-manager.md` no longer exists.
- No factory-shipped file (agents, skills, config) contains a reference to
  `chat-agent` or `kit-manager` as an agent name (historical references in
  proposals and review history are excluded).
