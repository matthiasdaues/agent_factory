---
id: ST-0072
epic: Token-Efficient Workflow Continuity
title: Reconcile token-efficiency completion evidence
tier: strong
status: done
deps: [ST-0065, ST-0066, ST-0067, ST-0068, ST-0069, ST-0070, ST-0071]
traces: [UC-11, UC-12, FR-K1, FR-K2, FR-K3, FR-K4, FR-K5, FR-K6, FR-K7, FR-L1, FR-L2, FR-L3, Session Transcript Proposal, Dispatch Efficiency Proposal]
outputs: [docs/reviews/token-efficiency-completion.md, docs/reviews/dispatch-safeguard-audit-2026-08-04.md, docs/proposals/agent-dispatch-token-efficiency.md, docs/proposals/proposal-session-transcript-token-control.md]
---

# Reconcile token-efficiency completion evidence

Close both separately traceable proposal origins only after their accepted
criteria map to passing evidence, including a measured post-adoption session.

**Priority:** must-have

## Acceptance Criteria

- Every completion criterion in both accepted proposals maps exactly once to a
  passing contract, runtime, test, or deliberately shared story outcome.
- The six-row dispatch audit is updated with passing automated evidence and no
  retrospective safeguard reimplementation.
- A retrospective from a session using phase gating reports its
  provider-qualified late/early input ratio and compares it with 11.3x without
  treating the comparison as a live control.
- Proposal status and references reflect observable delivered state only.
- The completion report preserves session-control assurance `elevated` with
  `reliability`/`operations`, and dispatch assurance `high` with
  `reliability`/`data_integrity`.

## Implementation Notes

This final serial story owns both proposal files, preventing status-edit
overlap with implementation stories. Reforecast only if the recorded evidence
changes estimate confidence under the accepted governance rules.

## Analysis

ST-0072 closes the accepted Session Transcript Proposal and Dispatch
Efficiency Proposal through UC-11, UC-12, FR-K1 through FR-K7, and FR-L1
through FR-L3. The completion report is the single reconciliation surface: its
two criterion tables assign each of the ten session criteria and seven dispatch
criteria exactly once to an observable passing contract, runtime, test, shared
story outcome, or measured retrospective. The accepted scope, separate design
origins, deferred live controls, and both governance classifications remain
unchanged.

The planning output list omitted the six-row dispatch audit that the second
acceptance criterion explicitly requires this story to update. Before editing
that audit, this analysis amends the declared outputs to include
`docs/reviews/dispatch-safeguard-audit-2026-08-04.md`. The audit update changes
only assurance dispositions and evidence links; it does not reimplement any
of the six shipped safeguards.

**Test seams.** The highest-level acceptance seam is the durable Markdown
contract: both proposal frontmatter statuses, exactly 10 and 7 uniquely keyed
completion rows, six passing audit rows, preserved governance fields, and one
measured retrospective section. Existing focused suites provide behavioral
evidence at the public boundaries for handoff linting, phase transitions,
bounded child envelopes, derived usage signals, dispatch preflight and
pre-merge gates, background permission argv, and nested/scope contracts. The
red baseline is observable because the completion report is absent, both
proposals remain `accepted`, and all six audit rows remain verified gaps.

The retrospective uses the real post-adoption Codex implementation session
`019fcc1a-1421-7862-8039-f649f3cf030b`, which entered from the authoritative
Planning-to-Implementation handoff. Its persisted post-ST-0071 checkpoint
records a `full-cache` capability and a late/early input ratio of
`1.7358340957174099`. The record's provider field is unavailable (`null`), so
the report retains `cli=codex`, `provider=unavailable`, model
`gpt-5.6-sol`, and capability rather than guessing a provider. The comparison
with 11.3x is retrospective evidence only and neither controls the live run nor
claims causation.

No specification or architecture change is needed: implementation conforms to
the accepted UC-11/UC-12 contracts, including BR-042's nullable provider
metadata and retrospective-only usage signals. No new architecture decision is
introduced.

## Verification

- The eight focused contract/runtime modules pass: 99 tests.
- `backlog-lint`, `spec-lint`, `arch-lint`, `matrix-lint`,
  `statemachine-lint`, `transition-lint`, and `index-lint --check` pass with no
  errors. Reported warnings and informational findings do not concern ST-0072.
- The full orchestrator suite has 589 passing tests and one pre-existing,
  unrelated failure:
  `test_FAGAN0009_design_records_the_implemented_schema_boundary` expects
  `factory/docs/design/research-survey-mode.md`. That file is absent from the
  exact declared base `5af30f21b1e86f25982344566533d5486912a153` and is
  outside this story's scope.
- Spec feedback found no drift and no new architecture decision: the accepted
  UC-11/UC-12 and BR-042 contracts already describe the delivered behavior.
