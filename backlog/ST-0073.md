---
id: ST-0073
epic: Test Execution Hooks
title: Wire pre-push full-suite test gate into canonical pre-commit config
tier: economy
status: done
deps: [ST-0009]
traces: [UC-09, ADR-0003, RECON-0017]
outputs: [factory/config/pre-commit-config.yaml, factory/scripts/init-factory, factory/README.md, orchestrator/tests/test_init_factory_prepush_hook.py]
---

# Wire pre-push full-suite test gate into canonical pre-commit config

Add the missing pre-push test gate to `factory/config/pre-commit-config.yaml`
— the canonical config `init-factory` splices into consumer projects — so the
"ready-to-share" full-suite gate that ADR-0003 (accepted, unsuperseded) and
`factory/README.md` already document is actually installed. This closes open
defect RECON-0017: the documentation is the intended truth, the configuration
drifted. `factory/scripts/run-tests` (built by ST-0009) already supports
`--full`; the hook configuration and hook-type installation are absent.

**Priority:** must-have

## Acceptance Criteria

- `factory/config/pre-commit-config.yaml` gains a new hook inside its existing
  `- repo: local` block with `id: agent_factory_hook-run-tests-full` (the
  `agent_factory_hook-` prefix is the removal key and must be preserved).
- The hook sets `entry: factory/scripts/run-tests --full`,
  `language: system`, `stages: [pre-push]`, `pass_filenames: false`, and
  `always_run: true`, with a brief comment naming it the "ready-to-share" gate
  and accurately noting the human `git push --no-verify` bypass.
- Running
  `merge-precommit-config --target <consumer>.pre-commit-config.yaml --template factory/config/pre-commit-config.yaml`
  carries the pre-push hook into the consumer config alongside the existing
  hooks; a second invocation is an idempotent no-op (the already-merged marker
  still keys on the template's first hook id).
- `init-factory` into a fresh consumer project produces a
  `.pre-commit-config.yaml` that contains `agent_factory_hook-run-tests-full`
  with `stages: [pre-push]` (asserted by a new init-factory test).
- `init-factory` installs both the `pre-commit` and `pre-push` Git hook types;
  adding a pre-push-stage YAML entry alone does not make Git invoke it.
- Behaviorally: on an ordinary `git push`, `run-tests --full` runs; exit `0`
  lets the push proceed, while exit `1`/`2` blocks it with test failures or the
  framework error on stderr. A human can explicitly bypass the client-side
  hook with `git push --no-verify` (corrected UC-09 extension 1c).
- `factory/README.md`'s three-point test-regime description (pre-commit,
  pre-push, phase advance) matches what is actually installed: the pre-push
  point (point 2) now matches the canonical config, and a confirmation pass
  records that every described test hook is wired and every wired test hook is
  described. Any residual drift in point 1 (the pre-commit `--changed-only`
  test hook, which is also absent from the canonical config) is resolved in
  this same change or filed as a separate finding — never left silent.

## Investigation Notes

- **Originating defect:** RECON-0017 (severity major, category defect,
  artifact `factory/config/pre-commit-config.yaml`, status open). It records
  that ADR-0003 and `factory/README.md` § "Test execution hooks" both describe a
  pre-push hook running `run-tests --full` that blocks `git push` on failure,
  but the canonical config defines no `pre-push`-stage hook and no `run-tests`
  entry at all (`grep run-tests factory/config/pre-commit-config.yaml` returns
  nothing). This repo's own `.pre-commit-config.yaml` has only the pre-commit
  `run-tests --changed-only` hook; no `stages: [pre-push]` entry exists
  anywhere. ADR-0003 is accepted and unsuperseded, so the config — not the docs
  — is what drifted. RECON-0017 is the originating defect reference; no
  duplicate finding is filed.

- **Why the config propagates without merge-script changes.** `merge-precommit-config` splices the entire
  `- repo: local` block from the template into a consumer's
  `.pre-commit-config.yaml`, keyed on the template's first hook id
  (`agent_factory_hook-mdformat`). Placing the new pre-push hook inside that
  same block means `init-factory` / `merge-precommit-config` carry it
  automatically, so the merge script needs no change. Implementation review
  found that `pre-commit install` installs only the default hook type, however;
  `init-factory` must explicitly request both `pre-commit` and `pre-push` for
  Git to invoke the new stage. The existing
  `test_precommit_block_prefixed_and_on_top` init-factory test continues to
  hold.

- **Supersedes ST-0013.** ST-0013 ("Configure pre-push hook to run full test
  suite", status pending) previously proposed a raw git hook at
  `.github/hooks/pre-push` installed via `factory/scripts/install-hooks`, and
  carries a status correction noting it was never built. (An orphan
  `.github/hooks/pre-push` script does exist on disk, but it is untracked,
  not symlinked into `.git/hooks/`, and not carried to consumers by
  `init-factory` — so it is not the live mechanism the README describes.)
  RECON-0017 redirects
  the implementation to the pre-commit-framework `pre-push` stage, which is
  consistent with how the existing test and lint hooks are wired and with
  ADR-0003's mechanically triggered gate and the README's three-point
  description. ST-0073 supersedes ST-0013's approach; closing/superseding
  ST-0013 is an implementation-side bookkeeping step for this story (it is not
  listed in `deps` because ST-0013 is itself unbuilt and must not block this
  fix).

- **Adjacent drift (out of scope, flagged).** The canonical config also lacks
  the pre-commit `--changed-only` test hook (README point 1); that hook exists
  only in this repo's own merged `.pre-commit-config.yaml`. RECON-0017 scopes
  its fix to the pre-push full-suite gate, so this story wires point 2 only.
  The README consistency acceptance criterion above requires that drift to be
  either closed in the same change or filed as its own finding, so the
  three-point description never silently overstates what is installed. Point 3
  (phase advance) is already enforced by the FSM `tests_pass` entry condition
  and needs no config change.

- **Traceability.** UC-09 (Run Tests via Hook) extensions 1c (ordinary pre-push
  blocks on failure; human `--no-verify` bypass) and 5a (failure exit `1`, JSON summary) and BR-026
  (`--full` runs the complete suite) define the behavior. ADR-0003 fixes the
  pre-push hook as the default ready-to-share full-suite gate.

## Analysis

### Implementation

- Add `agent_factory_hook-run-tests-full` to the existing local hook block in
  `factory/config/pre-commit-config.yaml`. It will invoke
  `factory/scripts/run-tests --full` at the `pre-push` stage, receive no file
  names, and run for every push attempt handled by pre-commit.
- Update `init-factory`'s installation command to request both `pre-commit` and
  `pre-push` hook types. This corrects the planning assumption that YAML wiring
  alone activates a new Git hook type.
- Add `orchestrator/tests/test_init_factory_prepush_hook.py` as a focused
  integration contract. It will initialise a fresh synthetic consumer and
  assert that the installed `.pre-commit-config.yaml` contains the complete
  UC-09/BR-026 pre-push hook shape. A second scenario will exercise
  `merge-precommit-config` against a consumer-owned config and prove both
  propagation and idempotence.
- Recheck `factory/README.md` against the installed canonical hook set. Point 2
  will require no prose change once the hook is wired. Point 1 remains
  inaccurate because the canonical template lacks the `--changed-only` hook;
  this implementation will file that residual drift separately rather than
  silently broadening RECON-0017.
- Mark ST-0013 superseded by ST-0073, mark RECON-0017 resolved after the tests
  pass, and set ST-0073 to `done` in the implementation commit.

### Files

- Modify `factory/config/pre-commit-config.yaml`.
- Modify `factory/scripts/init-factory`.
- Create `orchestrator/tests/test_init_factory_prepush_hook.py`.
- Modify `backlog/ST-0013.md`, `backlog/ST-0073.md`, and
  `docs/findings/RECON-0017.md` for completion bookkeeping.
- Create a separate reconciliation finding for the canonical template's
  missing pre-commit `run-tests --changed-only` hook. Modify
  `factory/README.md` only if the confirmation pass finds wording that can be
  made more accurate without changing the accepted three-gate contract.

### Test scenarios and seams

- **UC-09 pre-push installation:** observe the public `init-factory` boundary
  and assert that a fresh consumer receives the full-suite hook with
  `stages: [pre-push]`, `pass_filenames: false`, and `always_run: true`.
- **UC-09 hook activation:** observe `init-factory`'s pre-commit installation
  boundary and assert that it requests both Git hook types.
- **UC-09 propagation/idempotence:** observe the public
  `merge-precommit-config` boundary and assert that a consumer-owned hook is
  preserved, the full-suite hook is carried alongside it, and a second merge
  is a byte-for-byte no-op.
- Hook exit behavior is delegated unchanged to pre-commit and
  `factory/scripts/run-tests`; this config-only story does not add another
  execution wrapper to unit-test.

### Concern requiring agreement

Git itself accepts `git push --no-verify`, which bypasses the `pre-push` hook.
The proposed pre-commit-framework stage therefore supplies a default blocking
gate but cannot satisfy UC-09 extension 1c and ADR-0003 literally where they
say the gate has "no bypass" or is "unavoidable." No local client-side Git hook
can enforce that property against a repository user who controls the client.
The user confirmed the practical client-side hook and requested correction of
the inaccurate absolute wording. The specification and architecture are
updated in this story; server-side protected-branch or required-CI enforcement
remains outside scope.
