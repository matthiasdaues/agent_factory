---
id: ST-0282
epic: Diagnosed Installation
title: Complete a remote installation with digest verification and receipt
tier: standard
status: pending
deps: [ST-0281]
traces: ["scope-map#Newcomer installs a verified Factory release with explicit consent"]
touches: ["packages/factory/scripts/", "tests/factory/"]
concerns: {technical: [Factory source and packaging]}
quality-gates: []
---

# Complete a remote installation with digest verification and receipt

**Priority:** must-have

## Goal

A newcomer runs `install-agent-factory --from-remote <URL> --target <path> --version <ver>` and the bootstrap resolves an immutable versioned release URL, verifies `agent-factory.tar.gz` against `SHA256SUMS` before extraction, and presents a preview including the resolved URL and digest. A missing or mismatched digest blocks extraction and leaves the target unchanged.

## Contract

| Operation                                                   | Input state                                | Outcome                                                                                                                                    | Persistence                                                                            |
| ----------------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| `install-agent-factory --from-remote <URL> --version <ver>` | Valid URL, version exists on remote        | Resolves immutable versioned release URL from `<URL>/releases/<ver>/`                                                                      | No file changes until consent                                                          |
| Download and verify archive                                 | Remote archive matches `SHA256SUMS` digest | Verification passes; extraction proceeds                                                                                                   | Archive ready for installation                                                         |
| Download and verify archive                                 | Archive digest missing from `SHA256SUMS`   | Extraction refused; non-zero exit                                                                                                          | Target unchanged                                                                       |
| Download and verify archive                                 | Archive digest differs from `SHA256SUMS`   | Extraction refused; non-zero exit                                                                                                          | Target unchanged                                                                       |
| Remote installation preview                                 | Verification passed                        | Preview shows resolved URL, verified digest, source, version, target, interfaces, affected paths, instruction files, and uninstall command | No file changes                                                                        |
| Approved remote installation                                | Affirmative consent after preview          | Receipt confirms remote source, digest, and changed paths                                                                                  | Factory tree installed; `.agent-factory/install.json` records remote source and digest |

## Required Script Behavior

### Remote URL resolution

The bootstrap resolves assets from a distribution remote with this layout:

- `<base>/latest` — HTTP 302 redirect to `<base>/releases/<latest-version>/`.
- `<base>/releases/<version>/install-agent-factory`
- `<base>/releases/<version>/agent-factory.tar.gz`
- `<base>/releases/<version>/SHA256SUMS`

When `--version <ver>` is given, the bootstrap fetches directly from `<base>/releases/<ver>/`. When `--version` is omitted, the bootstrap follows the `<base>/latest` redirect to discover the current version.

### Download and verification

The bootstrap uses `urllib.request.urlopen()` (stdlib, Python 3.8+). No external dependencies.

1. Download `SHA256SUMS` from the resolved version URL.
2. Download `agent-factory.tar.gz` from the same version URL.
3. Compute SHA-256 of the downloaded archive.
4. Compare against the digest in `SHA256SUMS`. If missing or mismatched, refuse extraction, report the expected and actual digests, and exit non-zero.
5. On match, proceed to the installation preview.

Default timeout per request is 30 seconds. Network failures (connection refused, timeout, DNS resolution failure, HTTP 4xx/5xx) produce a message naming the URL and the error, then exit non-zero without changes.

### Manifest fields

The manifest records `"source_selector": "remote"`, `"resolved_source": "<resolved URL>"`, and `"archive_sha256": "<verified digest>"`. These fields join the `"source_selector"` and `"resolved_source"` keys introduced by ST-0281.

## Boundaries

- All edits go to `packages/factory/`. Never edit `.agent-factory/factory/`.
- The archive must not be extracted before digest verification passes.
- No fallback to unverified extraction when the digest check fails.
- The local installation path from ST-0281 is not modified. The remote path extends the bootstrap.
- Default HTTP timeout is 30 seconds per request. No retry logic.

## Affected Paths

- `packages/factory/scripts/install-agent-factory` — existing bootstrap (from ST-0281), extended with remote source handling.
- `tests/factory/test_install_agent_factory.py` — existing test file (from ST-0281), extended with remote-specific tests.

## Inputs

- Spec: `docs/spec/value-first-onboarding-journey.feature` — Rule "Newcomer installs a verified Factory release with explicit consent", Scenarios "Remote release resolves to immutable verified assets", "Missing or mismatched digest blocks extraction", and "Installation preview names every planned effect" (remote content).
- Supplementary: `docs/spec/supplementary_specs/validation-rules.md` — VFO-010 through VFO-015 (remote verification rules).
- Supplementary: `docs/spec/supplementary_specs/interface-contracts.md` — install-agent-factory contract table (remote columns).
- Architecture: `docs/arc42/05_building_block_view.md` — Distribution container.
- Glossary: `docs/arc42/12_glossary.md` — entries for `Distribution Remote`, `install-agent-factory`.
- Deps: ST-0281 delivers the bootstrap script with local installation, preflight, consent, receipt, and header management. This story extends the bootstrap with remote source handling.
- Ownership: this story owns the contracts for remote resolution and digest verification, the remote extraction gate, and complete preview content covering both local and remote sources (from EPIC 2 Ownership Resolution table).

## Demo Scenario

1. Run `install-agent-factory --from-remote https://releases.example.com --target ./project-2 --version 1.0.0`.
2. Preflight reports readiness without changing files.
3. The preview shows the resolved release URL, verified digest, source, version, target, interfaces, affected paths, instruction files, and uninstall command.
4. Approve the installation.
5. The receipt confirms the remote source, digest, and changed paths.
6. Replace `SHA256SUMS` with a file containing a wrong digest. Run `install-agent-factory --from-remote https://releases.example.com --target ./project-3 --version 1.0.0`.
7. The bootstrap reports the digest mismatch and refuses to extract the archive. The target is unchanged.

## Demo Data

None. The demo exercises the bootstrap against a remote distribution endpoint and a target project directory.

## Evidence

- [ ] `--from-remote <URL> --version <ver>` fetches from `<URL>/releases/<ver>/` without following a redirect.
- [ ] `--from-remote <URL>` without `--version` follows the `<base>/latest` 302 redirect to the current version.
- [ ] The bootstrap downloads `SHA256SUMS` and `agent-factory.tar.gz` via `urllib.request.urlopen()`.
- [ ] A SHA-256 mismatch between the downloaded archive and `SHA256SUMS` blocks extraction and exits non-zero.
- [ ] A missing digest entry in `SHA256SUMS` blocks extraction and exits non-zero.
- [ ] Network failures (timeout, HTTP 4xx/5xx, DNS) produce a message naming the URL and error, then exit non-zero.
- [ ] The preview shows the resolved URL and verified digest.
- [ ] The manifest records `"source_selector": "remote"`, `"resolved_source"`, and `"archive_sha256"`.

## Verification

```bash
uv run pytest tests/factory/test_install_agent_factory.py -v --tb=short
```

## Out of Scope

- Local source handling — ST-0281 implements the local installation path.
- Prerequisite fix loop — ST-0283 handles consent-gated fixes.
- Release asset building — ST-0280 produces the release asset set.
- Distribution remote hosting — external infrastructure outside Factory.
- First-session behavior — EPIC 4 handles project insight and hook configuration.
- Update logic — EPIC 3 implements the update transaction.

## Stop Conditions

- Downloading from HTTPS requires TLS certificate handling beyond `urllib`'s system-cert defaults — report the requirement and ask.
- Code in `packages/factory/scripts/install-agent-factory` and the feature spec disagree on remote URL resolution or digest verification behavior — report the conflicting files and the decision needed; do not add a workaround.

## Resolve Before Implementation

1. What is the exact URL layout of the distribution remote? The spec describes `<base>/latest` (redirect) and `<base>/releases/<version>/` (versioned assets) — does `latest` redirect to the newest version directory, or does it contain the assets directly?

   > `<base>/latest` is an HTTP 302 redirect to `<base>/releases/<latest-version>/`. The bootstrap follows the redirect, then fetches the three assets (`install-agent-factory`, `agent-factory.tar.gz`, `SHA256SUMS`) from the resolved version directory. When `--version` is given, the bootstrap skips the redirect and goes directly to `<base>/releases/<version>/`. This matches how GitHub Releases' "latest" endpoint works across the three distribution remotes. Decided 2026-09-24.

2. How does the bootstrap download the archive and checksum? Does it use `curl`, `wget`, or another tool? What error handling is expected for network failures?

   > `urllib.request.urlopen()` — stdlib-only, no external dependencies. Network failures (connection refused, timeout, HTTP 4xx/5xx) produce a clear message naming the URL and the HTTP status, then exit non-zero without changes. Default timeout is 30 seconds. No fallback to `curl` or `wget`. Decided 2026-09-24.
