---
id: ST-0163
epic: arc42 Projection Gating
title: Reconcile rules and documentation for arc42.projected property
tier: economy
status: pending
deps: [ST-0159]
traces: []
outputs: [factory/rulebooks/rules.md, factory/skills/maintain-architecture/SKILL.md]
quality-gates: []
notes: "crap-score excluded: documentation files, no production code. dependency-check excluded: no imports."
---

# Reconcile rules and documentation for arc42.projected property

Update project rules and documentation to reflect the new `arc42.projected` property.

**Priority:** must-have

## Acceptance Criteria

- `factory/rulebooks/rules.md` architecture section has a MUST rule: provision `"arc42.projected" "false"` in workspace properties when creating a fresh `architecture.dsl`; only the architecture-agent sets it to `"true"` on explicit user request for chapter projection
- `factory/skills/maintain-architecture/SKILL.md` notes that first-time chapter projection requires flipping `arc42.projected` to `"true"` before committing
- `mdformat` passes on all modified files

## Implementation Notes

- No changes needed to `.pre-commit-config.yaml`, `validate` skill, or `atam-review` skill — the gating is transparent inside arch-lint
