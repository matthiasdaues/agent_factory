---
id: ST-0289
epic: Value-First Session
title: See a gate demonstration before choosing hooks
tier: standard
status: pending
deps: [ST-0287]
traces: ["scope-map#Newcomer sees gate value before choosing hooks"]
touches: ["packages/factory/scripts/", "tests/factory/"]
concerns: {technical: [Factory source and packaging]}
quality-gates: []
---

# See a gate demonstration before choosing hooks

**Priority:** should-have

## Goal

A newcomer who accepts the gate demonstration sees a real Factory gate report a failure on a disposable fixture, corrects the fixture, sees the gate pass, and sees the fixture removed without changing the target project. Skipping the demonstration does not change recommended hook defaults.

## Contract

| Operation                      | Input state                              | Outcome                                                                            | Persistence                               |
| ------------------------------ | ---------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------- |
| Accept gate demonstration      | First-session insight complete (ST-0287) | `hook-demo` creates a disposable fixture; a real Factory gate fails on the fixture | Fixture exists in temporary location      |
| Correct the fixture            | Gate reported failure                    | Same gate passes on the corrected fixture                                          | Fixture corrected in place                |
| Gate passes                    | Corrected fixture                        | `hook-demo` removes the fixture and verifies its absence                           | Fixture removed; target project unchanged |
| Skip gate demonstration        | First-session insight complete (ST-0287) | Session proceeds to hook configuration with the same recommended defaults          | No fixture created; no file changes       |
| Interrupt during demonstration | Fixture exists                           | `hook-demo` removes the fixture on exit                                            | Fixture removed; target project unchanged |

## Required Script Behavior

### Gate selection

The demonstration uses `mdformat --check` (pre-commit hook `agent_factory_hook-mdformat`). It operates on a single file and has clear pass/fail output.

### Fixture

A markdown file at `.current-work/hook-demo/demo-fixture.md` with a deliberate formatting error (e.g. inconsistent heading level). The fixture is owned by Factory, not derived from project files.

### Demonstration sequence

1. Create `.current-work/hook-demo/` and write the fixture with a formatting error.
2. Run `mdformat --check` on the fixture. Show the failure output.
3. Automatically correct the fixture (run `mdformat --number` on it).
4. Run `mdformat --check` on the corrected fixture. Show the pass output.
5. Remove `.current-work/hook-demo/` and verify its absence.

### Cleanup

The fixture and its directory are removed after the demonstration completes, whether it succeeds, is skipped, or is interrupted (signal handler or exception cleanup).

### Skip behavior

Skipping the demonstration does not change recommended hook defaults. The session proceeds directly to hook configuration.

## Boundaries

- The disposable fixture is owned by Factory, not created from project files.
- The gate is a real Factory gate, not a mock or simulation.
- The fixture and any temporary files are removed after the demonstration, whether the demonstration succeeds or is interrupted.
- The script does not require tool installation beyond what preflight already verified.
- All edits go to `packages/factory/`. Never edit `.agent-factory/factory/`.

## Affected Paths

- `packages/factory/scripts/` — new `hook-demo` script.
- `tests/factory/` — new `test_hook_demo.py`.

## Inputs

- ST-0287 deliverables: reordered first-session flow with project insight.
- Feature spec: [Rule "Newcomer sees gate value before choosing hooks"](../docs/spec/value-first-onboarding-journey.feature) — scenarios "Gate demonstration shows one failure-to-pass cycle" and "Skipping the gate demonstration preserves defaults".
- Validation rules: `docs/spec/supplementary_specs/validation-rules.md` — VFO-030.
- Existing gates: Factory gates in `.pre-commit-config.yaml` (117 lines) for candidate gate selection.
- Ownership: this story owns the contracts "Gate demonstration shows one failure-to-pass cycle" and "Skipping the gate demonstration preserves defaults" (from EPIC 4 Ownership Resolution table).

## Demo Scenario

1. Open a Factory session. The session reports project insight (ST-0287). Accept the gate demonstration.
2. The `hook-demo` script creates a disposable fixture (a temporary markdown file with a formatting error).
3. A real Factory gate (e.g., `mdformat --check`) runs against the fixture. The gate reports a failure.
4. The session shows the failure and corrects the fixture (or guides the newcomer to correct it).
5. The gate runs again. The gate passes.
6. The `hook-demo` script removes the fixture. Verify the fixture is gone and the target project is unchanged.
7. (Alternative) Skip the demonstration. Verify the recommended hook defaults are the same as without skipping.

## Demo Data

None. The `hook-demo` script creates and removes its own disposable fixture. No existing project data is needed.

## Evidence

- [ ] `hook-demo` creates `.current-work/hook-demo/demo-fixture.md` with a deliberate formatting error.
- [ ] `mdformat --check` fails on the fixture and the failure output is shown.
- [ ] The script corrects the fixture automatically with `mdformat --number`.
- [ ] `mdformat --check` passes on the corrected fixture and the pass output is shown.
- [ ] `.current-work/hook-demo/` is removed after the demonstration.
- [ ] The target project files are unchanged after the demonstration.
- [ ] Skipping the demonstration does not change recommended hook defaults.
- [ ] Interruption during the demonstration removes the fixture and its directory.

## Verification

```bash
uv run pytest tests/factory/test_hook_demo.py -v --tb=short
```

## Out of Scope

- Hook configuration — delivered by ST-0290.
- Context capture gate — delivered by ST-0288.
- Project insight — delivered by ST-0287.
- Full pre-commit hook management — the demonstration uses one gate; hook configuration is a separate story.

## Stop Conditions

- Code in `packages/factory/scripts/hook-demo` and the feature spec disagree on what the demonstration shows → report the conflicting files and the decision needed; do not add a workaround.

## Resolve Before Implementation

1. Which Factory gate should the demonstration use? `mdformat --check` is a candidate, but the choice affects fixture design.

   > `mdformat --check`. It operates on a single markdown file, has clear pass/fail output, and requires no tools beyond what preflight verifies. The fixture is a `.md` file with a deliberate formatting error (e.g. inconsistent heading levels or missing trailing newline). Decided 2026-09-24.

2. Should the script correct the fixture automatically, or should the newcomer correct it manually? The feature scenario says "the corrected fixture passes" but does not specify who corrects it.

   > The script corrects the fixture automatically. The newcomer observes the failure, sees the fix applied, and sees the gate pass. Manual correction would require teaching markdown formatting mid-onboarding — too much friction for a demonstration. Decided 2026-09-24.

3. Where should the disposable fixture live — under `.current-work/`, under the system temp directory, or in the project root?

   > Under `.current-work/hook-demo/`. This directory is inside the project, gitignored, and in Factory's managed space. init-factory includes `/.current-work/` in the gitignore block. The fixture path is deterministic and cleaned up after the demonstration or on interruption. Decided 2026-09-24.
