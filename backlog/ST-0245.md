---
id: ST-0245
epic: Query canonical session usage across four CLIs and by dimension
title: Apply four-CLI conservation rules and compute canonical session usage without duplication
tier: standard
status: pending
risk_level: medium
deps: [ST-0244]
traces: [local-usage-processing-and-analysis.feature#Local operator obtains conservative canonical usage without duplication, ADR-0015]
touches: [packages/usage/]
concerns: {technical: [Python toolchain]}
quality-gates: []
---

# Apply four-CLI conservation rules and compute canonical session usage without duplication

Deliver the Accounting Registry, latest-snapshot selection, and the `canonical_session_usage` DuckDB view implementing the four conservation rules.

**Priority:** must-have

## Goal

After this story ships, the operator runs `usage-query canonical_session_usage` on multi-CLI fixtures and sees Claude Code sessions add the latest root snapshot and each distinct child run exactly once, Pi sessions add the root and each distinct descendant exactly once, and Codex and Copilot sessions use the latest inclusive root snapshot only — children do not increase the total. An unknown CLI exits non-zero and names the unsupported value.

## Domain Rule

- Logical-run identity is the tuple `(cli, session_id, run_id)`. Source path, line, capture sequence, and record content do not enter the key.
- Parent run ID establishes ancestry without changing identity. Every snapshot for a logical run has the same parent run ID (including null).
- Latest snapshot selection: greatest capture sequence, then greatest normalized path (unsigned UTF-8 byte order), then greatest one-based line number.
- `claude-code` conservation: latest root snapshot plus each distinct child run exactly once.
- `pi` conservation: root record plus each distinct descendant run exactly once.
- `codex` conservation: latest inclusive root snapshot only. Child attribution records do not increase the total.
- `copilot` conservation: same as `codex`.
- Unknown CLI exits non-zero and names the unsupported value.

## Demo Scenario

1. Prepare fixture directories with Claude Code sessions (repeated root snapshots, duplicate child evidence), Pi sessions (nested descendants), Codex sessions (inclusive root with child attribution), and Copilot sessions (inclusive root with child attribution).
2. Run `usage-query canonical_session_usage --usage-dir fixtures/multi-cli/`.
3. Verify Claude Code sessions add the latest root snapshot and each distinct child exactly once.
4. Verify Pi sessions add the root and each distinct descendant exactly once.
5. Verify Codex and Copilot sessions use the latest inclusive root snapshot only — children do not increase the total.
6. Add a record with an unknown CLI. Run `usage-query canonical_session_usage`. Verify exit non-zero naming the unsupported CLI.

## Affected Paths

- `packages/usage/src/usage/accounting.py` — new file (Accounting Registry mapping CLIs to conservation rules, latest-snapshot selection)
- `packages/usage/src/usage/views/canonical_session_usage.py` — new file (DuckDB view applying conservation rules per session)
- `packages/usage/src/usage/cli.py` — existing file, add `canonical_session_usage` view routing
- `packages/usage/fixtures/multi-cli/` — new directory with synthetic four-CLI fixture matrix
- `packages/usage/tests/contract/test_accounting.py` — new file (contract tests for four-CLI conservation)

## Inputs

- ST-0244 deliverables: `capture_health` view, stable-view refusal, preflight valid/failure relations
- ST-0241 deliverables: registry module with closed CLI set
- Feature spec: `docs/spec/local-usage-processing-and-analysis.feature` — Rule: Local operator obtains conservative canonical usage without duplication (all CLI-specific scenarios, snapshot selection, unknown CLI)
- QA strategy: `docs/spec/local-usage-processing-and-analysis-qa-strategy.md` — contract owner LU-03, test ID LU-03-CT-01, boundary cases for each CLI
- Architecture §5.7: `docs/arc42/05_building_block_view.md` — Accounting Registry and Query Model v1 components
- Ownership: contract LU-03 (valid rooted ancestry, snapshot selection, and four-CLI conservation) is owned by this story

## Outputs

- `packages/usage/src/usage/accounting.py` — module containing:
  - Accounting Registry: data structure mapping `claude-code`, `pi`, `codex`, `copilot` to their conservation rules.
  - Latest-snapshot selection function: deterministic evidence identity using greatest capture sequence, then greatest normalized path (unsigned UTF-8 byte order), then greatest one-based line number.
  - Conservation rule functions: additive root-and-children (claude-code), additive root-and-descendants (pi), inclusive-root-only (codex, copilot).
- `packages/usage/src/usage/views/canonical_session_usage.py` — DuckDB view that applies the correct conservation rule per CLI for each session. Returns one row per canonical session with session totals.
- `packages/usage/src/usage/cli.py` gains `canonical_session_usage` view routing.
- `packages/usage/fixtures/multi-cli/` — synthetic JSONL fixture matrix covering all four CLIs with repeated roots, duplicate children, nested descendants, and inclusive roots.
- `packages/usage/tests/contract/test_accounting.py` — pytest contract tests with `@pytest.mark.spec("LU-03")` markers covering each CLI's conservation rule and snapshot tiebreaking.

## Required Behavior

The Accounting Registry maps each CLI to its conservation function. When `canonical_session_usage` processes a session partition:

1. Select the latest snapshot for each logical run using deterministic evidence identity.
2. Look up the session's CLI in the Accounting Registry.
3. Apply the conservation rule:
   - `claude-code`: sum the latest root snapshot and the latest snapshot of each distinct child run.
   - `pi`: sum the root record and the latest snapshot of each distinct descendant run.
   - `codex` / `copilot`: use the latest inclusive root snapshot. Do not add child attribution records.
4. If the CLI is not in the registry, exit non-zero and name the unsupported value.

## Constraints

- Conservation rules are pure SQL over DuckDB relations. Python owns parameter validation and registry lookup.
- The Accounting Registry consumes the closed CLI set from `registry.py` (ST-0241). Do not duplicate the set.
- Contract tests use pure SQL fixtures — no subprocess or filesystem operations.
- Do not modify the preflight or Input Snapshot components.

## Suggested Agent Plan

1. Create `packages/usage/src/usage/accounting.py` with the Accounting Registry, latest-snapshot selection, and conservation rule functions.
2. Create `packages/usage/src/usage/views/canonical_session_usage.py` with the DuckDB view applying conservation rules per session.
3. Update `cli.py` to route `canonical_session_usage` to the new view.
4. Create synthetic four-CLI fixtures in `packages/usage/fixtures/multi-cli/`.
5. Write contract tests in `packages/usage/tests/contract/test_accounting.py` with `@pytest.mark.spec("LU-03")` markers covering each CLI and tiebreaking.
6. Run `usage-query canonical_session_usage --usage-dir packages/usage/fixtures/multi-cli/` and verify per-CLI totals.

## Acceptance Criteria

- [ ] Claude Code sessions add the latest root snapshot and each distinct child exactly once (LU-03)
- [ ] Pi sessions add the root and each distinct descendant exactly once (LU-03)
- [ ] Codex sessions use the latest inclusive root snapshot only; children do not increase the total (LU-03)
- [ ] Copilot sessions use the latest inclusive root snapshot only; children do not increase the total (LU-03)
- [ ] Latest-snapshot selection uses greatest capture sequence, then greatest normalized path, then greatest line number (LU-03)
- [ ] Unknown CLI exits non-zero and names the unsupported value (LU-03)
- [ ] Contract tests carry `@pytest.mark.spec("LU-03")` markers (LU-03)

## Verification

**Contract tests:**

```bash
uv run pytest --tb=short --quiet tests/ -m "spec" -k "LU_03 or accounting"
```

**Full test suite:**

```bash
uv run pytest --tb=short --quiet tests/
```

**CLI check:**

```bash
packages/usage/scripts/usage-query canonical_session_usage --usage-dir packages/usage/fixtures/multi-cli/
```

## Out of Scope

- `raw_usage_snapshots`, `latest_run_snapshots`, `usage_by_dimension`, `cache_efficiency` views (ST-0246)
- Result format adapters (EPIC 4)
- Parquet export (EPIC 4)
- Component lifecycle (EPIC 5)

## Agent Stop Conditions

- If the DuckDB SQL required for conservation rules exceeds a single view definition and needs multiple intermediate CTEs, confirm the SQL structure before proceeding
- If the tiebreaking logic for unsigned UTF-8 byte order comparison is not straightforward in DuckDB SQL, stop and verify the correct approach

## Resolve Before Implementation

None — the four conservation rules, snapshot tiebreaking, and unknown-CLI behavior are fully specified.
