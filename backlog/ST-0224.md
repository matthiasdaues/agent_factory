---
id: ST-0224
epic: Propagate concern model and relocate testing.yaml
title: Update virgil, reconciliation-agent, and init-factory for the concern model
tier: standard
status: done
deps: [ST-0217, ST-0223]
traces: [factory-concern-oriented-agent-context.md]
touches: [packages/factory/agents/, packages/factory/scripts/, tests/factory/]
quality-gates: [crap-score, dependency-check]
---

# Update virgil, reconciliation-agent, and init-factory for the concern model

Update three critical factory consumers for the concern model. Virgil's fitting procedure references `agent-context.md` instead of YAML files. The reconciliation-agent's Step 6 becomes a concern-registry health check. Init-factory generates CLI-specific include directives for `docs/agent-context.md`.

**Priority:** must-have

## Demo

The user starts a fitting session. Virgil's step 2 invokes `capture-context` which produces `agent-context.md` (not YAML files). Virgil's fitting references confirm `agent-context.md` exists, not `stack.yaml`. The user runs the reconciliation-agent after implementation. Its Step 6 validates that every concern section in `agent-context.md` has valid `Read:` paths and that the concern vocabulary matches the project's documentation structure -- it does not reference `context-interview-guide.yaml`. The user runs `init-factory`. It generates `@docs/agent-context.md` in the Claude Code orientation file, an equivalent reference in `.github/copilot-instructions.md`, `.pi/instructions.md`, and `.codex/instructions.md`.

## Acceptance Criteria

- Virgil's `inputs:` list references `docs/agent-context.md` instead of `docs/agent-context/*.yaml` files.
- Virgil's fitting step 2 invokes the concern-model `capture-context` (producing `agent-context.md`, not YAML files).
- Virgil does not reference `docs/charter/` fallback paths.
- The reconciliation-agent's Step 6 validates that every concern section in `agent-context.md` has valid `Read:` paths and that the concern vocabulary matches the project's documentation structure.
- The reconciliation-agent does not reference `context-interview-guide.yaml` or YAML index files.
- `init-factory` generates CLI-specific include directives for `docs/agent-context.md`: `@docs/agent-context.md` for Claude Code, appropriate references for Copilot CLI, Pi, and Codex.
- `init-factory` does not generate include directives for YAML agent-context files or charter files.

## Scope

**Status quo:** Virgil's `inputs:` list references `docs/agent-context/stack.yaml`, `workflow.yaml`, `governance.yaml`, `testing.yaml` with `docs/charter/` fallbacks. The reconciliation-agent's Step 6 compares against `context-interview-guide.yaml` and YAML index files. `init-factory` generates CLI instructions referencing YAML agent-context. `testing.yaml` resolves at `docs/testing.yaml` (ST-0223 deliverables). Concern-lint and the rulebook exist (ST-0217 deliverables).

**Adds:** Virgil references `agent-context.md`. Reconciliation-agent health check validates concern sections. Init-factory generates include directives for `agent-context.md` per CLI type.

**Out of scope:** Mechanical sweep of all other agent and skill definitions (ST-0225). Testing.yaml path updates (already done in ST-0223).

## Notes for the Implementer

Virgil's inputs section currently lists four YAML files with charter fallbacks (lines 26-39). Replace with `docs/agent-context.md` and `docs/testing.yaml`. The reconciliation-agent references `context-interview-guide.yaml` at line 26 and line 110 -- both must be replaced with the concern-registry health check description. Init-factory's CLI template generation needs `@docs/agent-context.md` added to the Claude Code orientation file template and equivalent references for other CLIs.

**Quality-gate notes:** mutation-analysis excluded: virgil and reconciliation-agent changes are prose definition updates. init-factory changes are template string updates for include directive generation, not behavioral logic.
