---
id: ST-0034
epic: Research Playbook
title: Regenerate INDEX and close out required-test coverage
tier: standard
status: done
deps: [ST-0025, ST-0026, ST-0027, ST-0028, ST-0029, ST-0030, ST-0031, ST-0032, ST-0033]
traces: [Completion Criteria, Required Tests]
outputs: [factory/INDEX.yaml]
---

# Regenerate INDEX and close out required-test coverage

Regenerate `factory/INDEX.yaml` with `index-lint` so the new research agents,
skills, playbook, and policy rulebooks appear in the catalog, and confirm that
every required test and every completion criterion from the proposal maps to an
accepted story.

The `policies/` category is discovered automatically by `index-lint`, which
derives a rulebook's category from its parent directory. The JSON Schemas under
`schemas/` are data validated by `schema-validate`, not prose rulebooks, and
`index-lint` scans only `.md` files, so schemas do not appear in the catalog by
design. This story records that decision and verifies the regenerated index is
clean.

**Priority:** must-have

## Acceptance Criteria

- `factory/INDEX.yaml` is regenerated by `index-lint` and includes the four
  research agents, the six research skills, the research-topic playbook, and
  the four research policy rulebooks under the `policies` category.
- `index-lint` reports zero errors against the regenerated index.
- A coverage checklist maps each of the proposal's fifteen "Required Tests" to
  the story whose acceptance criteria prove it, with no test unmapped.
- A completion checklist confirms every item in the proposal's "Completion
  Criteria" is satisfied by an accepted story.
- The decision that `schemas/*.schema.json` are intentionally absent from the
  catalog is recorded, so a later reader does not treat the absence as a defect.

## Closeout Record

`factory/INDEX.yaml` was regenerated by `index-lint` on each feature-branch
merge and verified clean at closeout (`factory/scripts/index-lint --check`
reports "up to date", exit 0). The catalog now lists the four research agents
(phase 6, Research), the six research skills (category `research`), the
`research-topic` playbook, and the four research policy rulebooks under the
`policies` category.

### Schemas Absent By Design

`index-lint` scans only `.md` files and derives a rulebook's category from its
top-level directory under `rulebooks/`. The JSON Schemas under
`rulebooks/schemas/research/` are `.schema.json` data validated by
`schema-validate`, not prose rulebooks, so they are intentionally absent from
the catalog. A later reader must not treat their absence as a defect.

### Required-Tests Coverage

Each of the proposal's fifteen "Required Tests" maps to the story whose
acceptance criteria and automated tests prove it:

| #   | Required test                                       | Proven by                                                                                                                                                   |
| --- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Missing required fields fail schema validation      | ST-0018 (schema-validate) with per-artifact schemas ST-0019–ST-0022                                                                                         |
| 2   | An unfalsifiable claim cannot enter review          | ST-0020 (conjecture schema requires refutation condition) + ST-0031 (refutation-design states a claim with no refutation condition is not ready for review) |
| 3   | An author cannot review its own claim               | ST-0025 (`test_author_cannot_review_or_vote_on_own_claim`)                                                                                                  |
| 4   | Copied sources do not count as independent evidence | ST-0025 (`test_copied_sources_are_not_independent_evidence`)                                                                                                |
| 5   | A failed severe test blocks survival                | ST-0025 (`test_failed_severe_test_blocks_survival`)                                                                                                         |
| 6   | An invalid test does not support a claim            | ST-0025 (`test_invalid_test_does_not_support_a_claim`)                                                                                                      |
| 7   | A changed claim invalidates prior reviews and votes | ST-0025 (`test_changed_claim_invalidates_prior_reviews_and_votes`)                                                                                          |
| 8   | A new assumption starts a new review cycle          | ST-0025 (`test_new_assumption_starts_a_new_review_cycle`)                                                                                                   |
| 9   | A tie does not produce survival                     | ST-0025 (`test_a_tie_does_not_produce_survival`)                                                                                                            |
| 10  | Abstentions do not create a majority                | ST-0025 (`test_abstentions_do_not_create_a_majority`)                                                                                                       |
| 11  | A blocker prevents survival                         | ST-0025 (`test_a_blocker_prevents_survival`)                                                                                                                |
| 12  | A refuted claim cannot enter the report as fact     | ST-0025 (`test_refuted_or_unresolved_claim_cannot_enter_report_as_fact`)                                                                                    |
| 13  | An unsupported report statement fails validation    | ST-0025 (`test_unsupported_report_statement_fails_validation`)                                                                                              |
| 14  | Failed tests remain visible                         | ST-0025 (`test_failed_tests_remain_visible`)                                                                                                                |
| 15  | Required qualifications remain in the report        | ST-0025 (`test_required_qualifications_remain`)                                                                                                             |

No required test is unmapped.

### Completion-Criteria Coverage

| Completion criterion                                                | Satisfied by                                                        |
| ------------------------------------------------------------------- | ------------------------------------------------------------------- |
| `rulebooks/schemas/` exists                                         | ST-0018                                                             |
| Research artifacts have templates and schemas                       | ST-0019, ST-0020, ST-0021, ST-0022                                  |
| Playbook runs from brief to validated report                        | ST-0033                                                             |
| Every step declares its input and output artifacts                  | ST-0033                                                             |
| Outputs pass schema validation before the next step begins          | ST-0033 (gate) + ST-0025 (staged schema→policy→semantic entrypoint) |
| Role separation is enforced                                         | ST-0023 (policy) + ST-0025 (validator)                              |
| Each material claim states what would refute it                     | ST-0020 (schema) + ST-0031 (refutation-design)                      |
| Every surviving claim has traceable evidence, tests, reviews, votes | ST-0022 (claim-register) + ST-0025 (traceability checks)            |
| Every factual report statement maps to a surviving claim            | ST-0024 (report policy) + ST-0025 (report-stage checks)             |
| Refutations and uncertainty remain visible                          | ST-0024 (report/evidence policies) + ST-0025                        |
| All required tests pass                                             | Full suite green (270 passed at closeout)                           |
