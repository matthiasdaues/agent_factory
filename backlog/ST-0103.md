---
id: ST-0103
epic: Semantic Gate Scripts
title: Create dependency-check gate skill, script, and fixture
tier: standard
status: done
deps: []
traces:
  - "Proposal: Agentic Quality Gates § Design 1 — dependency-check"
outputs:
  - factory/skills/dependency-check/SKILL.md
  - factory/scripts/dependency-check
  - factory/fixtures/quality-gates/dependency-violation/
tests: []
---

# Create dependency-check gate skill, script, and fixture

Create the dependency-check gate: a skill document, a CLI wrapper script,
and a test fixture. The gate enforces module dependency directions declared
in `architecture.dsl`. Neither TDD nor the testing strategy addresses
dependency direction; this gate fills the gap.

**As a** implementation-agent dispatcher,
**I want** a deterministic dependency-check gate that validates import
directions against `architecture.dsl`,
**so that** architectural rules are enforced mechanically rather than
discovered during architecture review.

**Priority:** must-have

## Acceptance Criteria

01. `factory/skills/dependency-check/SKILL.md` exists and documents the
    dependency-rule enforcement gate.
02. `factory/scripts/dependency-check` is an executable script that reads
    `docs/arc42/architecture.dsl` and checks source imports against declared
    dependency rules.
03. The script produces a JSON report per rule: rule name, pass/fail,
    violating import (file, line, imported module).
04. Reports are logged to
    `.current-work/dependency-check/<story-id>.json`.
05. Default threshold: 0 violations (architectural rules are binary).
06. The script exits non-zero on any violation.
07. Tool candidates: `deptrack` (general), `dependency-cruiser` (JS/TS),
    `arch-pkg` (Go).
08. `factory/fixtures/quality-gates/dependency-violation/` contains a
    self-contained test project with: an `architecture.dsl` declaring
    module A must not depend on module B, a source file in A importing B
    (expected violation), and a conforming import (expected pass).
09. The script runs against the fixture and produces the expected results.
10. All outputs pass `factory/scripts/validate`.

## Implementation Notes

**Inputs:**

- `docs/proposals/implemented/agentic-quality-gates-and-specification-consolidation.md`
  § Design 1 (dependency-check section), § Test Fixtures

**Strategy:** create skill doc, CLI script, and fixture. The script parses
dependency rules from `architecture.dsl` and validates source imports.

**Tier rationale:** standard. Requires DSL parsing, import graph analysis,
and fixture design with known violations. No dependency on the other two
gate stories.

## Analysis

- Implement a new `dependency-check` gate as a documented skill, an
  executable Python CLI, and a self-contained fixture with one forbidden
  import and one conforming import.
- Create or modify only these tracked outputs: `factory/skills/dependency-check/SKILL.md`,
  `factory/scripts/dependency-check`,
  `factory/fixtures/quality-gates/dependency-violation/`, and this story file.
- Cover the proposal dependency-check scenarios at two seams: invoking the
  CLI against the fixture and inspecting the JSON report it writes to
  `.current-work/dependency-check/<story-id>.json`.
- Concerns and ambiguities: the trace points to a proposal section rather
  than a use-case file, so the implementation follows the accepted design
  directly; the repository does not currently expose `factory/scripts/validate`,
  so verification must rely on the available underlying gate commands.

## Seams

- `factory/scripts/dependency-check` CLI exit status
- JSON report written to `.current-work/dependency-check/<story-id>.json`
- Fixture project under `factory/fixtures/quality-gates/dependency-violation/`
