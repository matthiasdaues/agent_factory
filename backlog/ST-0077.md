---
id: ST-0077
epic: Project Charter
title: Create capture-charter skill
tier: standard
status: done
deps: [ST-0074, ST-0075]
traces: []
outputs: [factory/skills/capture-charter/SKILL.md]
---

# Create capture-charter skill

Create the `capture-charter` lifecycle skill with three modes that scaffold, scan, and complete the project charter.

**Priority:** must-have

## Acceptance Criteria

- Skill file at `factory/skills/capture-charter/SKILL.md` with proper frontmatter
- `--init` mode: creates the three charter files from templates under `docs/charter/`, fills answers already known from the vision conversation, unfilled sections get "To be decided."
- `--init --scan` mode (brownfield): scans the codebase for existing configuration files (`pyproject.toml`, `package.json`, `docker-compose.yml`, `.github/workflows/`, linter configs, test configs, `.pre-commit-config.yaml`), pre-populates the charter skeleton, and presents findings for stakeholder confirmation
- Default mode (completeness sweep): walks every "To be decided" entry, forces a decision or explicit deferral, checks whether corresponding artifacts already exist, derives Epic 0 stories for missing artifacts, and writes them to `backlog/ST-NNNN.md` using the standard story format with `epic: "Epic 0 — Project Setup"`, `tier: economy`, MoSCoW `must-have`
- Completeness sweep creates a final "Update development.md with actual commands and paths" story that depends on all other Epic 0 stories
- The skill runs in the orchestrating session (not a spawned subagent)
- Skill references `charter-lint` for validation

## Implementation Notes

The completeness sweep's Epic 0 derivation checks for existing artifacts before creating stories. When an artifact exists, it creates a smaller "verify/adapt" story or no story. The stakeholder can point at existing work during the sweep. Epic 0 stories use the lowest available ST numbers.

## Analysis

This story produces a single prose artifact — a Markdown skill definition —
not application code, so there is no Use Case to trace and no unit under
test in the usual sense. `traces: []` is correct as shipped. The seam is
the skill file's content against three sources of truth: the accepted
proposal (`docs/proposals/implemented/capture-project-constraints.md`), the two
prerequisite artifacts it must reference (`factory/rulebooks/templates/charter-*.md`
from ST-0074, `factory/scripts/charter-lint` from ST-0075), and the sibling
skill `factory/skills/update-charter/SKILL.md`, which owns incremental
charter writes and must stay clearly distinguished from this skill's
create/scan/complete lifecycle.

Verification is by acceptance-criteria checklist against the written file,
plus the two deterministic gates already in the repo:
`factory/scripts/mdformat --number` (formatting) and
`factory/scripts/link-check` (the cross-reference to `update-charter`
resolves). `factory/scripts/index-lint` regenerates `factory/INDEX.yaml`
from this file's frontmatter automatically via the pre-commit hook — no
manual edit needed.

Confirmed against the acceptance criteria:

- Frontmatter: `name: capture-charter`, `category: requirements`,
  `version: 1.0.0`, `disable-model-invocation: false` — present.
- `--init` mode documented: create three files from templates, fill known
  vision-conversation answers, leave the rest `To be decided.`, validate
  with `charter-lint`, commit.
- `--init --scan` mode documented: scan table covering every file named in
  the acceptance criteria (`pyproject.toml`, `package.json`,
  `docker-compose.yml`, `.github/workflows/`, linter configs, test
  configs, `.pre-commit-config.yaml`), pre-populate, present findings for
  stakeholder confirmation, no Epic 0 derivation.
- Default mode (completeness sweep) documented: walk every "To be
  decided." entry to a decision or explicit deferral, check for existing
  artifacts before deriving Epic 0 stories (missing-vs-exists table),
  write stories in standard format with the required frontmatter, and
  always write the closing "Update development.md with actual commands
  and paths" story with `deps:` on every other Epic 0 story written in the
  same sweep.
- Orchestrating-session constraint stated explicitly at the top of the
  file, not just implied by mode descriptions.
- `charter-lint` referenced in every mode (structural mode after `--init`
  and `--init --scan`; `--planning-gate` mode as part of the completeness
  sweep's own completion criteria) plus a consolidated validation-reference
  table at the end.
