---
id: ST-0107
epic: Architecture Routing
title: Update feature-addition.md Step 0.3 with mechanical routing
tier: economy
status: done
deps: [ST-0106]
traces:
  - "Proposal: Agentic Quality Gates § Design 4 — Effect on workflow"
outputs:
  - factory/playbooks/feature-addition.md
tests: []
quality-gates: []
notes: "Gate opt-out: output is playbook documentation only; no application code changes, so the default semantic gates (crap-score, mutation-analysis, dependency-check) have no code to measure."
---

# Update feature-addition.md Step 0.3 with mechanical routing

Update the feature-addition playbook's Step 0.3 routing decision to include
the mechanical module-graph check between the declaration and the decision.

**As a** playbook orchestrator,
**I want** Step 0.3 to run the module-graph check script before deciding
Phase 2 routing,
**so that** architecture phase routing is driven by the module graph's
actual state, not solely by manual declaration.

**Priority:** must-have

## Acceptance Criteria

1. `feature-addition.md` Step 0.3 includes a "Mechanical check" step that
   runs `factory/scripts/module-graph-check` at the end of Phase 1, before
   Phase 3 starts.
2. The mechanical check's output updates the proposal's
   `impact.architecture_change` field per the override semantics.
3. The playbook documents that the mechanical check uses Phase 1 outputs
   only — it does not depend on story files or implementation artifacts.
4. The playbook documents that the reconciliation-agent catches any
   module-graph changes missed by the Phase 1 check.
5. The routing decision uses the (possibly updated) field value to enter
   or skip Phase 2.
6. All changes pass `factory/scripts/validate`.

## Implementation Notes

**Inputs:**

- `factory/playbooks/feature-addition.md` — current playbook
- `docs/proposals/implemented/agentic-quality-gates-and-specification-consolidation.md`
  § Design 4 (Effect on workflow, Enforcement)

**Strategy:** amend Step 0.3 to add the mechanical check step and update
the routing logic description.

**Tier rationale:** economy. Prose amendment to an existing playbook —
insert a step and update the routing decision.
