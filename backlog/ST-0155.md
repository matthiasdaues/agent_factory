---
id: ST-0155
epic: Charter Layer Bindings
title: Update kit-manager to populate layer bindings in testing.yaml
tier: standard
status: pending
deps: [ST-0148]
traces: [UC-09]
outputs: [factory/agents/kit-manager.md]
quality-gates: []
notes: "crap-score excluded: agent definition file, no production code. mutation-analysis excluded: no production code. dependency-check excluded: no imports."
---

# Update kit-manager to populate layer bindings in testing.yaml

Update `factory/agents/kit-manager.md` to populate the `layers` section of `docs/charter/testing.yaml` during charter completeness sweep and brownfield onboarding. The kit-manager scans repository test infrastructure (conftest.py, test directories, Makefile targets, runner configs) and records layer bindings.

**Priority:** should-have

## Acceptance Criteria

- `factory/agents/kit-manager.md` includes a step in Step 2 (lines 84-91) to populate layer bindings from repository scan
- The kit-manager scans conftest.py, test directories, Makefile targets, and runner configs
- Layer bindings record: tool, infrastructure, entry_point, and optional anti_patterns per layer
- The fidelity map is populated when infrastructure characteristics are identifiable
- A human reviewer confirms the bindings match the repository's actual test infrastructure
- Unused layers are omitted, not set to null
- `mdformat` passes on the modified file

## Implementation Notes

- This shares scan surface with the `detect-test-regime` skill (ST-0153) but runs in a different context (charter completeness sweep vs. onboarding)
- Layer names use the vocabulary from `factory/rulebooks/conventions/testing-strategy.md`
- Tier is standard per dispatch-contract tier rubric: default tier, no simpler rule matches
