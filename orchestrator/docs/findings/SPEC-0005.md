---
id: SPEC-0005
source: spec-review
severity: major
category: defect
artifact: docs/spec/actor-goal-list.md
status: resolved
traces: [AG-13, UC-08, UC-05]
---

# AG-13 is a User Goal that no use case realizes

**What is wrong:** AG-13 ("Inspect run status through dedicated views") is listed as a User Goal in `actor-goal-list.md`, but no use case declares `Realizes: AG-13`, so `spec-lint` reports a `TRACE001` coverage gap. The Enhanced status views requirements in `system-use-cases.md` cite `UC-08, AG-13`, yet UC-08 declares `Realizes: AG-08` only. The goal has no realizing use case and therefore no acceptance scenarios of its own.

**Fix:** Give AG-13 a realizing use case. The simplest option is to add `AG-13` to UC-08's `Realizes:` line, since UC-08 already covers navigating to and displaying the status views; alternatively, extend UC-05 to cover the four dedicated views and declare `Realizes: AG-13` there. Re-run `spec-lint` and confirm the `TRACE001` notice for AG-13 is gone.
