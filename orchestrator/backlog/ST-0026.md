---
id: ST-0026
epic: Call-to-Action Prompts
title: Update run-step handler to pass standalone InvocationContext
classification: trivial
status: done
deps: [ST-0023, ST-0024]
traces: [ADR-0014, T-24, UC-01]
outputs: [src/orchestrator/cli.py]
---

# Update run-step handler to pass standalone InvocationContext

Update `_handle_run_step()` in `cli.py` to construct `InvocationContext(phase="standalone", role=AgentRole.AUTHOR, iteration=0)` and pass it to `PromptComposer.compose()`. This selects the standalone call-to-action template.

**Priority:** must-have

## Acceptance Criteria

- `run-step` passes a standalone `InvocationContext` to the composer
- The composed prompt includes the standalone call-to-action: "Execute the workflow defined in your Agent Definition above."
- Existing `run-step` tests updated for the new `compose()` signature
