---
id: ST-0064
epic: Orchestrator Reintegration
title: Flag ADR-0010 for supersession in the todos ledger
tier: economy
status: done
deps: [ST-0062]
traces: [ADR-0010]
outputs: [docs/spec/todos.md]
---

# Flag ADR-0010 for supersession in the todos ledger

`orchestrator/docs/adr/0010-separate-tooling-from-project-directory.md`
describes the old agent_hq distribution model — a global clone,
`uv tool install`, symlinks into each project directory. That no longer
matches agent_factory's actual model, where `factory/` is copied wholesale
into a project by `init-factory`. The real shipping/distribution mechanism
for orchestrator hasn't been designed yet (out of scope for this round), so
ADR-0010 is not rewritten now — it is flagged as a todo for whenever that
design work happens. Depends on ST-0062: the ledger this entry is added to
lives at `orchestrator/docs/spec/todos.md`, which doesn't exist at that
path until the docs move lands.

**Priority:** could-have

## Acceptance Criteria

- `orchestrator/docs/spec/todos.md` gains a new numbered todo entry (next
  available `T-NN`, following the file's existing numbering) titled around
  "ADR-0010 needs superseding".
- The entry states: ADR-0010 documents the pre-pivot agent_hq distribution
  model (global clone, `uv tool install`, project-directory symlinks),
  which does not match agent_factory's current `factory/`-copied-wholesale
  model.
- The entry states the trigger for resolving it: once orchestrator's real
  shipping/distribution mechanism is designed, supersede ADR-0010 with a
  new ADR reflecting that mechanism.
- The entry does not itself rewrite or supersede ADR-0010 — no change to
  `orchestrator/docs/adr/0010-separate-tooling-from-project-directory.md`.

## Analysis

**Task**: Add a new numbered todo entry (T-41) to `orchestrator/docs/spec/todos.md` documenting that ADR-0010 describes an obsolete distribution model (pre-pivot agent_hq: global clone + `uv tool install` + project-directory symlinks) that no longer matches agent_factory's current model (factory/ copied wholesale into projects via init-factory). Entry flags ADR-0010 for supersession once orchestrator's real shipping/distribution mechanism is designed.

**Seams**: Documentation-only work; no tests or code changes. Follow existing format of T-39 and T-40 in todos file (Status, Origin, prose body). Entry must not modify ADR-0010 itself, only reference it in the ledger.

**Acceptance Criteria Trace**:

- AC1: New numbered entry (T-41) added to `orchestrator/docs/spec/todos.md` — next number after T-40
- AC2: Title around "ADR-0010 needs supersiding"
- AC3: Entry states the obsolescence context — pre-pivot model, current model mismatch
- AC4: Entry states trigger for resolution — when real mechanism designed, supersede ADR-0010 with new ADR
- AC5: No change to ADR-0010 itself — only ledger entry, no modifications to the ADR file
