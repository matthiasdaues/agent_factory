---
id: ST-0035
epic: Working-Tree Gate
title: Update tests for working-tree gate model
tier: strong
status: done
deps: [ST-0027, ST-0028, ST-0029, ST-0030]
traces: [ADR-0013, T-25, UC-01, UC-02]
outputs: [tests/test_gate_runner.py, tests/test_phase_runner.py, tests/test_integration_requirements.py]
---

# Update tests for working-tree gate model

Update all test files that reference `PreCommitGateRunner` to use `WorkingTreeGate`:

1. **test_gate_runner.py**: Rewrite to test `WorkingTreeGate.verify()` with all four quadrants (exit_code × tree_state). Remove tests for staging, committing, and pre-commit output parsing.
2. **test_phase_runner.py**: Update mock gate to return results matching `verify()` semantics. Add confabulation detection tests. Add clean-tree-before-retry tests.
3. **test_integration_requirements.py**: Update integration test to work with agents that commit their own work.

**Priority:** must-have

## Acceptance Criteria

- All existing tests pass with the new gate model
- Confabulation detection is tested (exit 0 + dirty tree → halt)
- Clean-tree-before-retry is tested
- No references to `PreCommitGateRunner` remain in test code
- Integration test exercises the full working-tree gate flow
