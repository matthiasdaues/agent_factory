---
id: ST-0030
epic: Working-Tree Gate
title: Update run-step handler for working-tree gate
classification: trivial
status: done
deps: [ST-0027, ST-0028]
traces: [ADR-0013, T-25, UC-01, VR-025]
outputs: [src/orchestrator/cli.py]
---

# Update run-step handler for working-tree gate

Update `_handle_run_step()` in `cli.py` to use `WorkingTreeGate.verify()` after the agent subprocess exits, replacing the current `PreCommitGateRunner.run()` call. Map the gate result to the correct exit codes:

- `passed` → exit 0
- confabulation → exit 2 (VR-025)
- failure → exit 1

**Priority:** must-have

## Acceptance Criteria

- `run-step` uses `WorkingTreeGate.verify()` instead of `PreCommitGateRunner.run()`
- Exit code 2 for confabulation detection
- Exit code 1 for agent failure
- Existing run-step tests updated
