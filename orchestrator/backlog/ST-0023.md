---
id: ST-0023
epic: Working-Tree Gate
title: Add InvocationContext entity and update PromptComposer port
classification: trivial
status: done
deps: []
traces: [ADR-0014, T-24, UC-01, UC-02]
outputs: [src/orchestrator/entities.py, src/orchestrator/ports.py]
---

# Add InvocationContext entity and update PromptComposer port

Add the `InvocationContext` frozen dataclass (`phase: str`, `role: AgentRole`, `iteration: int`) to `entities.py`. Update the `PromptComposer` protocol in `ports.py` to require `InvocationContext` as a parameter on `compose()`, replacing the current optional-findings-only signature. This is a breaking protocol change — all callers must be updated in subsequent stories.

**Priority:** must-have

## Acceptance Criteria

- `InvocationContext` is a frozen dataclass in `entities.py` with `phase`, `role`, `iteration` fields
- `PromptComposer.compose()` signature accepts `InvocationContext` as a required parameter
- `findings` remains optional on `compose()`
- Existing unit tests for entities pass (no behavioural change to other entities)
